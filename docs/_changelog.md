# Changelog (append-only)

2026-09-09 — governance: C3 era gate review imported; C3 era CLOSED (four-lens review at docs/governance/reviews/c3-era-gate.md)

2026-07-29 — docs: documentation foundation created (Phase 0 prompt A)
2026-07-29 — docs: imported Pass 1 specification set (6 documents, verbatim)
2026-07-29 — ci: guard skeleton (build/typecheck/lint, gitleaks, migration linter with self-test, string scan warn-mode, bundle report)
2026-07-29 — fix: lint-clean scaffold files; exclude generated files from lint/format (INC-001)
2026-07-29 — db: countries reference table (migration 0001, applied + verified); migrations rulebook added
2026-07-29 — governance: Phase 0 gate CLOSED; Knowledge v3.1 mirrored to repo; Phase 1 spec session open
2026-07-30 — db: identity function hardening; deny-proofs D1–D7 executed and recorded
2026-07-30 — i18n: runtime + en/am locales + language switcher (Phase 1 P1-b)
2026-07-30 — i18n: keyed scaffold error/not-found strings (INC-002); string-scanner promoted to fail-mode
2026-07-30 — auth: email sign-in door (signup, signin, email verification, callback) — Phase 1 P1-c
2026-07-30 — auth: browser client set to PKCE flow (detectSessionInUrl) for reliable email-link session exchange (INC-004 follow-up)
2026-07-30 — auth: revert to implicit flow; fix callback success/session logic (INC-004) and check-email sign-in navigation (BUG 2)
2026-07-30 — auth: resend throttle + honest states (INC-005), URL-driven auth views (BUG 2b), DEBUG panel removed
2026-07-30 — auth: remove editable resend-to input (D-004 drift, abuse vector); URL-driven sign-in/sign-up mode (BUG 2c)
2026-07-30 — auth: check-email view live-detects confirmation (same-browser) + forward guidance (cross-device); no more dead-end (INC-005 completion)
2026-07-30 — auth: consolidate check-email actions (D-005)
2026-07-30 — auth: iOS same-browser confirmation detection via storage rehydration (INC-005 final)
2026-07-30 — auth: session-smart already-confirmed path; INC-005 closed with best-effort auto-flip ruling
2026-07-30 — security: scan rulings documented in-schema; function grants re-verified (INC-006)
2026-07-30 — docs: formatting debt resolved; records exempted from tooling, living docs CI-covered (INC-007)
2026-07-30 — fix: format-check red main (CI-exact command) — INC-007 final
2026-07-30 — deps: audit ruled dev-only-accepted; no change (Bun override not scopeable); eslint upgrade + audit-gate tracked (INC-008)
2026-07-30 — docs: format slip fixed (INC-008 follow-up)
2026-07-30 — ci: pin prettier exact + deterministic format:check script (INC-009)
2026-07-30 — security: adversarial auth test pass documented (P1-c capstone)
2026-07-30 — security: remove arbitrary-email resend abuse vector from /auth/callback (INC-010a)
2026-07-30 — security: auth adversarial capstone finalized — all pass or ruled; INC-010a fixed, INC-010b ruled (P1-c capstone)
2026-08-01 — test: add Playwright E2E harness (staging-targeted) + CI e2e job; frozen first spec
2026-08-01 — ci: fix E2E webServer — serve the built Worker with wrangler instead of `vite preview`
2026-08-01 — ci: point E2E wrangler serve at dist/server/wrangler.json (fixes fresh-checkout startup failure)
2026-08-01 — ci: E2E webServer switched to dev-server mode (Option B); e2e job no longer builds; debug step removed
2026-08-01 — test: E2E spec waits for signed-in state (Sign out button) before identity assertions (mobile-360 race)
2026-08-01 — test: E2E global-setup made self-verifying and fail-fast (preflight log, create/read-back/profile-trigger proofs)
2026-08-01 — test: E2E global-setup removes RLS-protected profiles-row check; trigger correctness covered by P1-a deny-tests
2026-08-01 — test: E2E sign-in fills are self-checking (toHaveValue guards, anchored locators) — empty-email submit fixed
2026-08-01 — test: E2E cold-start sign-in waits for hydration and retry-fills both controlled fields until stable
2026-08-01 — docs: catch-up — P1-c closure + INCs, DEC-009/010/011 + REQ-036/037/038, gap analysis, E2E harness acceptance, launch-gate checklist, system-state to current, v1.3 recorded
2026-08-01 — governance: Thread 1 handoff imported to handoffs/ (Phase 1, P1-c closed, E2E harness accepted, pre-P1-d)
2026-08-02 — ci: add CI status reporter workflow (workflow_run of CI → writes docs/tracking/ci-status.md, [skip ci]); CI push trigger ignores the status file
2026-08-02 — ci: exempt machine-generated docs/tracking/ci-status.md from prettier (INC-011); document CI status reporter capability
2026-08-02 — test: auth-door E2E backfill (A-1..A-3, B-1..B-4, C-1..C-4); shared e2e helpers; namespace-sweep teardown
2026-08-02 — test: gate sign-up E2E cases behind E2E_EMAIL_SINK (INC-013); restore routeTree SSR types; drop orphan i18n key (INC-012)
2026-08-02 — fix: relocate SSR Register type augmentation out of generated routeTree (INC-014)
2026-08-02 — ci: enable sign-up E2E cases via E2E_EMAIL_SINK; staging SMTP now a Mailtrap sandbox (INC-013 resolved)
2026-08-02 — test: fix A-3 virtual-clock mechanism (INC-015); add sign-up error diagnostics (INC-016)
2026-08-02 — fix: resend cooldown engages on click, not on success (INC-017); pace E2E sign-up sends (INC-018)
2026-08-03 — test: A-3 uses clock.runFor, not fastForward, so the cooldown countdown completes (INC-019)
2026-08-03 — test: A-3 resend exhaustion moves to a nightly real-time E2E job with heartbeat (INC-020)
2026-08-03 — test: guard-proof workflow proves B-3 and C-4 bite against mutation fixtures
2026-08-03 — test: guard-proof now proves both directions per guard via JSON-parsed A/B (INC-021)
2026-08-03 — fix: guard-proof writes JSON report via PLAYWRIGHT_JSON_OUTPUT_NAME (INC-021 follow-up)
2026-08-03 — docs: guard proof recorded as passed; thread 2 handoff imported
2026-08-03 — governance: DEC-012 re-scopes the phase ladder; Telegram door moves to a later named phase
2026-08-03 — feat: Google door (P1-d) — minimal scopes, enumeration-safe link refusal, Turnstile seam, scope-creep E2E guard
2026-08-03 — fix: country_source gains 'unknown'; remove US sentinel from handle_new_user; correct fabricated rows (INC-022)
2026-08-03 — fix: G-1 intercepts our first-hop authorize request, fulfilled not aborted (INC-023); Google spec mobile-only
2026-08-03 — docs: P1-d closed — D-8/D-10 deny evidence recorded, D-9 deferred-named, launch-gate re-run item added
2026-08-03 — feat: P1-f settings surface — identity read-only, sign-in methods with server-enforced last-method guard, password/email change, sign out other devices
2026-08-03 — fix: anchor sign-out locators (S-3); settings nav entry; relative-time formatter to lib
2026-08-03 — test: U-1..U-3 identity-unlink deny evidence via admin script (P1-f)
2026-08-03 — fix: unlinking the email identity now kills its password; existing ghost credentials corrected (INC-024)
2026-08-03 — docs: P1-f closed — settings surface deny-proven; INC-024 fix verified on both databases
2026-08-03 — feat: P1-g truth model — has_password()/remove_own_password(), password as its own sign-in method, neutral-always reset request
2026-08-03 — feat: P1-g password recovery — /auth forgot view, /auth/reset landing, R-2/R-3/S-4 E2E
2026-08-03 — test: A-1+A-2 merged to one sign-up; nightly A-3 gated on E2E_EMAIL_SINK; remove_own_password deny proof added
2026-08-03 — ci: enforcing dependency-audit job (fails on high/critical, and separately on an unreachable advisory service)
2026-08-03 — fix: P1-g probe workflow re-raises a failing phase-2 verdict instead of masking it (Step P)
2026-08-04 — chore: same-major overrides clear the 8 high dependency-audit findings (INC-025)
2026-08-04 — chore: recovery-identity probe retired (superseded by ruling R2; guarded by E2E R-2/S-4)
2026-08-04 — test: b3 guard-proof fixture regenerated against the moved auth surface
2026-08-04 — governance: PHASE 1 CLOSED — gate evidence recorded; handoff #3 imported; launch-gate swept
2026-08-04 — fix: forgot-password resubmit gains the INC-017 cooldown + per-visit cap (INC-026a); nightly E2E sink flag reconciled with ci.yml (INC-026b)
2026-08-04 — ci: nightly heartbeat push regenerates after fetch and retries; test outcome alone decides the job (INC-027)
2026-08-04 — feat: P2-a geography — locations tree (active-only RLS, ET+US shallow seed); Phase 2 opened
2026-08-04 — feat: P2-b categories + attribute schema (three-concept model, starter ethio.com seed)
2026-08-04 — docs: INC-029 — reported staging locations drift dismissed (prod is 32 rows: 2/12/18; "18" was the city count); no cleanup, re-runnability lesson recorded
2026-08-04 — feat: P2-c listings core — state machine, screening seam + bypass guard, private photo bucket (strip-gated), per-category expiry
2026-08-04 — feat: listings.tier column (LIVE v1 ranking lever) + partial feed-order index (P2-c-tier)
2026-08-04 — feat: design foundation — unified AppShell, config-driven permission-gated panels, coffee-on-cool-slate oklch tokens, Inter/Bricolage/Noto Sans Ethiopic, brand mark + spinner, Marketplace feed shape
2026-08-04 — fix: gitleaks prose false positive fingerprinted (INC-030); feed fails soft and contained so no feature outage cascades through the shell (INC-031)
2026-08-04 — fix: E2E specs repointed at the AppShell's real structure + hydration-aware navigation helpers (INC-032, INC-033); 44px tap-target floor enforced on header/footer chrome (INC-034); mobile/perf/security assertions added to shell.spec; marketplace weight guard added to CI
2026-08-04 — feat: layout revision — warm/cream purge to cool slate (gold restricted to logo dot + Featured badge), corner-block grid, exact logo lockup FIT, minimal top bar with expandable search, 3-column centered footer, recursive rail submenus, pre-paint dark-mode toggle
2026-08-04 — ci: first-paint bundle budget guard (gzipped JS/CSS ceiling, ratcheted)
2026-08-04 — feat: final layout refinement — corrected vertical stack (top bar → panel tabs → location row → clickable breadcrumbs → body), minimized top bar with full-width mobile search row, panel tabs replacing the bar dropdown (signed-in only), built-visible cascading location row (filtering stubbed), clickable category breadcrumbs, self-drawing spinner, compact centred footer, tighter rail rows, tightened logo lockup
2026-08-05 — fix: layout correction pass — top bar fills grid row 1 (aligned with the logo cell), responsive breakpoint lg→md so tablets get the persistent rail + full controls (real search field, language name, labelled account) and only phones minimize, footer as equal 3-column grid with halved row spacing, location row shows the resolved area once, breadcrumb root Home returns to the Marketplace feed + panel (tab still "Marketplace")
2026-08-05 — fix: shell stabilization — rail-collapse state unified on the `data-rail` attribute (INC-036), search-row logical-position utilities corrected so 360px no longer overflows (INC-037), E2E locators repointed at the moved identity and rail surfaces (INC-038); local pw config gitignored
2026-08-05 — fix: layout polish — descriptive per-category rail icons (INC-039), rail-collapse toggle moved to the top bar (INC-040), location selector rebuilt as a strict Country → Region → City → Sub-city cascade with no duplicated area label (INC-041), sidebar hover states moved onto the sidebar token family (INC-042)
2026-08-05 — fix: layout polish round 2 — breadcrumb root no longer repeats "Marketplace" (INC-043), feed body centred with equal gutters (INC-044), wordmark relocates to the top bar when the desktop rail collapses (INC-045), single sidebar affordance per breakpoint confirmed (INC-046), tighter footer rows with intact 44px tap boxes (INC-047)
2026-08-05 — fix: CI red + render-walk pass — footer tap-target assertion repointed at the real footer link (INC-051), collapsed-rail bar brand unified as the mark-less two-line lockup (INC-048), bar search width capped so the language control is never crowded at tablet width (INC-049), category tree cached with skeleton rows while loading (INC-050)
2026-08-05 — fix: shell panel-scoped chrome — location row gated to the Marketplace panel; category-rail purity and mobile collapse-toggle absence locked by E2E (INC-052 – INC-054).
2026-08-05 — fix: CI red + render-walk — collapse toggle genuinely hidden below md via cn()/twMerge display-conflict resolution (INC-055), panel-tabs row fits instead of scrolling (INC-056), category-tree caching re-confirmed (INC-057), marketplace-rail purity re-asserted incl. the mobile drawer (INC-053).
2026-08-05 — fix: shell panel/route sync — /settings now renders inside the Account panel context, not the marketplace rail (INC-058); top-bar controls right-aligned at md+ (INC-059); admin gating re-confirmed unchanged.
2026-08-05 — docs: thread-3 handoff + performance strategy + posting-flow spec + posting foundations build plan imported.
2026-08-05 — ci: CI Status Reporter regenerate-after-fetch + retry (staleness class, third strike; mirrors nightly INC-027 fix)
2026-08-07 — docs: DEC-013 resequencing ratified (Phase R RBAC-first; F/G named); REQ-039/040 adopted; gap register created; S25 + INC-060/061 recorded
2026-08-08 — feat(rbac): Phase R1 — RBAC core schema/functions/triggers/seeds + superadmin bootstrap (DEC-013, REQ-030)
2026-08-09 — fix(rbac): R1a — lock down log_audit/helper EXECUTE grants (INC-062); cascade-aware base-role guard (INC-063)
2026-08-09 — feat(rbac): R2 — admin policies retrofit across catalog/listings/profiles via has_permission (Phase R, DEC-013)
2026-08-09 — feat(rbac): R2b — append-only audit trigger; audit perms pruned to view/export; per-command RBAC policies; INC-064 probe portability; definer-grant CI guard

- 2026-08-09 — Phase R3: RBAC client seam (`src/features/permissions`), `/admin` route with redirect gate, permission-driven Admin panel tab, browse-path CI guard with self-test, RBAC E2E (`e2e/rbac.spec.ts`), and the function-matrix proof migration applied to ethio-prod. Docs: `docs/features/rbac-client-seam.md`.
- 2026-08-09 — test(rbac): R3a — assign/revoke success-path + audit write proofs (closes the Phase R function matrix)
- 2026-08-09 — fix(ci): changelog list-prefix red-main repair; pin bun 1.3.14 (INC-009 determinism; INC-065)
- 2026-08-09 — docs: Phase R gate record — S26, four-lens close-out review, system-state updated (DEC-013 Phase R complete)
- 2026-08-09 — feat(catalog): A1 — 96-category taxonomy (15 roots) + 54 attribute definitions with pointer-walking inheritance (get_category_attributes), icons + image seats (DEC-013, REQ-017/020; Apex donor, metric adaptations)
- 2026-08-10 — fix(catalog): A1b — operator-directed restructure: Vehicles intermediate under Automotive carries vehicle attrs (parts no longer inherit them); Computers + Phones under Electronics; 6 legacy dupe roots retired; 13 roots / 97 categories (INC-066)
- 2026-08-10 — feat(catalog): A2 — listing_locations with single-country trigger law, owner-managed RLS + admin read, cascade + deny proofs (DEC-013, F2)
- 2026-08-10 — feat(catalog): A2b — exact-pin columns (lat/lng/map_visible) with pair/visibility/range laws + owner UPDATE; INC-067 policy split (anon path free of has_permission)
- 2026-08-10 — feat(profile): A3 — seller alias (unique, validated), contact methods (phone/telegram/whatsapp + visibility laws), default post location for prefill; column-grant extension proven (F12/F13, personal-only v1)
- 2026-08-10 — docs: Phase A gate record — S27, four-lens close-out, system-state updated; Phase R gate stamped (staging addenda)
- 2026-08-10 — docs: DEC-014 ratified — foundations-first admin epoch (U0–U8) before the wizard; REQ-041 (tags) adopted; current target U0
- 2026-08-10 — Phase U0: admin shell & navigation — /admin converted to a layout route with permission-gated section nav (7 sections), breadcrumbs, deep-link guard with a translated refusal notice, and i18n'd empty-state pages; new src/features/admin/\*\*, admin child routes, EN+AM strings (AM provisional), e2e/admin-shell.spec.ts. No migrations.
- 2026-08-10 — fix(e2e): admin-shell setup aligned to user_roles 4-col constraint + assertions matched to real component semantics; docs: admin-shell feature doc (H1)
- 2026-08-10 — fix(e2e): signIn helper waits for established+persisted session (INC-068 root cause of U0 A-1 red); class rules recorded
- 2026-08-10 — fix(e2e): attemptSignIn split for expected-failure paths (INC-068 addendum) — six mobile-360 auth-negative tests re-anchored; signIn keeps the session contract
- 2026-08-12 — fix(admin): U0b — admin sections integrate the shell panel rail/drawer like sibling panels; internal sidebar removed; E2E extended (INC-069)
- 2026-08-12 — fix(shell): U0c — single functional breadcrumb with current-segment emphasis; Back-button removed; drawer redesigned to active-panel + switcher (operator render-walk)
- 2026-08-12 — fix(shell): U0d — admin landing cards restored (INC-070); panel-header band (name + switcher) below the logo on all screens; corner-block geometry untouched

- 2026-08-12 — fix(shell): U0e — panel switching navigates (INC-058/071); dead admin state-placeholder removed; drawer logo block divider + top-bar-height restored
- 2026-08-12 — feat(shell): U0f — fixed panel header with scrollable item region (drawer + rail, short-viewport E2E); testids on all rail items; drawer-scoped switcher selectors (two red tests repaired)
- 2026-08-12 — fix(ci): nanoid override >=3.3.18 (GHSA-2v37-7h3g-55p8, audit clean); md+ rail scroll test given a guaranteed-overflow premise (Admin panel at 1280×360)
- 2026-08-12 — fix(shell): desktop rail sized to viewport minus the 4rem logo row (sticky top-16, calc(100dvh-4rem)) — pinned Sign out back in view; U0f complete
- 2026-08-12 — test(e2e): U0f rail invariant corrected to viewport-minus-row-1 geometry (top 64px, height 296px at 1280×360)
- 2026-08-16 — feat(shell): U0g — desktop layout laws: fixed top band + fixed rail, footer full-width beneath the rail, content-only scrolling; E2E invariants L1–L3
- 2026-08-16 — fix(shell): U0g-2 — fixed top band + fixed rail (never move; inner list scrolls), footer full-width beneath the rail with readable inset content; L1/L2 test premise repaired
- 2026-08-16 — fix(shell): U0g-3 — the document is the only page scroller at md+ (content stack unbounded; band/rail fixed out of flow); L1/L2 precondition now satisfied
- 2026-08-16 — test(e2e): U0g layout laws on a real tall fixture route (injection harness retired after three flaky rounds); shell layout unchanged
- 2026-08-16 — fix(shell): U0h — footer paints over the fixed rail at desktop bottom (rail ends at footer top, both modes); test(e2e): i18n chrome-coverage guard (Amharic shell, zero fallbacks) with am.ts gaps filled
- 2026-08-16 — feat(catalog): provisional name_am for all 97 categories (pre-native-review); fix(shell): rail clamps above the in-view footer (last items + scrollbar always reachable); test(e2e): i18n-coverage repairs (brand allowlist, dialog locator, category-label coverage)
  2026-08-16 — test(e2e): rail geometry tests re-anchored to the footer-clamp law (top 64, bottom = min(viewport, footerTop)); i18n reds await the name_am staging apply
- 2026-08-16 — fix(shell): footer-inset hook flushes on scrollend (no one-frame lag); test(e2e): L3 asserts the clamp law by geometry with a settled measurement (compositor hit-test retired)
- 2026-08-16 — fix(auth): U0j — sign-out hard reset with confirmation, live auth guard on gated routes, permission cache purge, replace-navigate to marketplace; E2E SO-1..SO-4 (INC-072, Tier A)
- 2026-08-16 — test(e2e): U0j-2 — shared signOutViaUi with confirm; live-guard proof via same-tab client signOut (test hook under DEV) + reload path; strict-mode scoping; legacy sign-out sites re-anchored
- 2026-08-16 — fix(shell): footer clamp inset rounds up + re-clamps on content resize (locale switch); Sign out always clickable (hit-test law); test(e2e): locale-agnostic openRailScope
- 2026-08-16 — feat(auth): U0k — one-click sign-out (confirm removed); role-tiered idle/absolute session policy with warning + hard reset; cross-tab enforcement; sign-out-others on password change; E2E SP-1..SP-4 (Tier A, INC-072 addendum)
- 2026-08-16 — feat(shell): U0l — category selection navigates to /c/<slug> (rail Links, URL-derived highlight + breadcrumb, INC-073); /auth is a proper page; page-card standard (PageCard/PAGE_MAIN_CLASS); E2E C-1..C-4
- 2026-08-16 — fix(shell): U0l-2 — breadcrumb Home navigates via Link; header reads signingOut to prevent stale account-menu (SO-2); settings.tsx migrated to multi-card PageCard standard
- 2026-08-16 — docs: U0 gate record — S28, four-lens close-out, system-state updated
- 2026-08-16 — feat(admin): U1 Users — account_status rider + status guard trigger + seam enforcement; admin_list_users/get/set_status/activity RPCs (profiles:view/update gated); list + detail UI with role assignment via audited RPCs; E2E AU-1..AU-6 (Tier A, DEC-014)
- 2026-08-16 — fix(db): grant restatement for re-declared seams (definer-guard law); ci: migration-parity preflight before E2E (names the missing file); docs: INC-074 + guard law
- 2026-08-17 — fix(db): U1 migration self-describing (four seam grant lines restated in-file, definer guard green); docs: INC-074 addendum — admin-users AU-1/2/3/5 diagnosis DEFERRED, CI artifact and staging E2E credentials unreachable from the executor environment
- 2026-08-17 — fix(e2e/ui): admin-users AU-1/2/3/5 — switchUser helper (sign out before second sign-in; /auth is guarded when authenticated), distinct responsive testids in users-list; INC-074 addendum
- 2026-08-17 — feat(admin): U1b — DataTable primitive (priority-responsive, no-overflow law) + users list migrated; admin role gains profiles:update (seed); E2E table law; INC-075
- 2026-08-17 — test(e2e): switchUser settles auth state before branching (INC-074 addendum)
- 2026-08-17 — feat(ui): U1c — display primitives (DEC-015): StatCard/StatGrid, ChartFrame, FormSection, DetailPanel, DataTable slots; /dev/primitives fixture; primitives law suite (test-once responsiveness); preflight ledger-first (seed-only migrations detected)
- 2026-08-17 — fix(repo): INC-076 — U1c primitives/fixture/law-suite/preflight restored verbatim from f62f2f8 after a stale-checkout push overwrote main; base-SHA rule adopted
- 2026-08-17 — fix(ui): U1d — DataTable rows clickable on desktop (+law L8); own-row status controls hidden; settings deactivation banner; Users > <name> crumb; INC-077
- 2026-08-17 — ci: E2E failure reporter (docs/tracking/e2e-last-failure.md) + no-unexplained-deletions guard (INC-076); fix(auth): sign-out purges all auth-derived queries (SO-2/SO-4 after settings banner, INC-078)
- 2026-08-17 — feat(auth): U1f — TOTP MFA enrollment + step-up gate (AAL2) for requires_step_up permissions; server-side enforcement in mutation RPCs; E2E MF-1..MF-5 (Tier A)
- 2026-08-17 — ci: parity preflight — ledger via definer RPC (service_role only), loud degraded mode, object-probe verified against U1f; migration_marks law if no ledger (INC-074 follow-up)
- 2026-08-17 — ci: parity ledger via public.migration_marks (self-marking migrations; SQL-editor applies have no tool ledger); backfill; guard + law; preflight reads marks
- 2026-08-17 — feat(admin): U1g — edit user (admin_update_profile, step-up gated, audited) + parity table; test(e2e): AU-3/4/5 re-anchored to step-up (enrollAndStepUp), AU-9..11
- 2026-08-17 — fix(e2e): expectAal2 reads data.currentLevel; fix(auth): auth-derived query root + cancel-then-remove purge (SO-4 after U1g; INC-078 addendum)
- 2026-08-17 — fix(shell): neutral query-keys module (browse-path guard green); no permissions query registered when signed out (SO-4 purge assertion); header settles to Sign in after reset
- 2026-08-17 — fix(auth): hard reset unmounts the permissions loader before purge + gcTime 0 (SO-4 strict assertion); test(e2e): shared switchLanguage helper (selector drift)
- 2026-08-17 — ci: E2E sharded ×4 + merged report, cancel superseded runs, @smoke fail-fast tier, cache checks (≈11 min → ≈3–4 min); test(e2e): smoke re-anchored to Amharic labels
- 2026-08-17 — ci: per-process E2E fixture scoping (setup/teardown by run+shard id); namespace sweep moved to nightly; INC-080
- 2026-08-17 — ci: smoke tier uploads results + log tail on failure; merged report labels sources and quotes non-test crashes (reporter blind spot closed)
- 2026-08-17 — fix(e2e): minted fixture emails unique per worker (TEST_WORKER_INDEX + random suffix) — smoke tier no longer collides across projects (INC-080 addendum)
- 2026-08-17 — test(e2e): openRailScope hydration-gated + single bounded retry (mobile drawer flake under parallel load); drawer-open helpers unified
- U1f-4 — step-up gate verifies factor presence + verification freshness server-side (INC-081); client mirrors the 10-minute window; MF-6/MF-7 added.
- 2026-08-17 — test(e2e): testEmail delegates to mintEmail (unique per worker) — auth-signup collision under parallel workers; INC-080 closed
- 2026-08-17 — test(e2e): email-sending specs isolated (serial project/job) + rate-limit-aware assertion (INC-082); switcher tests routed through openRailScope
- 2026-08-19 — test(e2e): 429 watcher scoped to sign-up phase (resend 429 is the throttle under test); openRailScope waits out slow drawer animation (retry only on no-mount); drawer opens unified; ci: actions runtime bumps
- 2026-08-19 — ci: reporter names timeout-without-step deaths (last-steps block); test(e2e): helper waits throw named errors on exhaustion; drawer opens fully unified
- 2026-08-19 — docs: U1 gate record — DEC-015/DEC-016, S29, four-lens close-out, system-state + launch-gate updated
- 2026-08-22 — feat(admin): U2 Roles & Permissions console — matrix with system/is_core locks, custom-role lifecycle, DEC-016 permission registration, roles:view→admin; step-up on every mutation; E2E RP-1..8 (Tier A)
- 2026-08-22 — fix(admin): U2a — delete-confirm shows the role key adjacent (testid split); matrix vocabulary i18n EN+AM (+coverage guard); members link via ?role= URL param; test(e2e): roleRow twin helper, RP-9/RP-10; ci: reporter quotes error-context.md (JSON-steps retired)
- 2026-08-22 — ci: failure reporter — artifact-layout fix from local repro, never-silent wrapper (a reporter crash writes its own report), download resilience, layout fixtures (INC-084e)
- 2026-08-22 — ci: root-anchor test-results ignore; context fixtures actually committed (git-tracked, proven); self-test names its searched paths on 0-found (INC-084f)
- 2026-08-22 — ci: reporter matches error-context by full titlePath (describe-nested slugs) + real describe fixture in the self-test; test(e2e): expectSignedIn/signOutViaMenu anchor on account-menu-sign-out testid (INC-084g)
- 2026-08-22 — docs: U2 gate record — S30, four-lens close-out, INC-084 reconciled, system-state updated
- 2026-08-22 — feat(rbac): U2b DEC-017 — assignable-scope flag server-enforced; matrix locks escalation-vector permissions and badges user-baseline rows (derived live)
- 2026-08-22 — feat(admin): U3 Audit & Security — audit viewer (stats + 14-day chart + filters/pagination) and impersonation v1 (super-admin only, step-up, 15-minute box, dual-actor audit, global banner, READ-ONLY; auth-level act-as deferred)
- 2026-08-22 — style(docs): prettier admin-audit-security.md (format:check was omitted from the U3 pre-flight — checklist reminder)
- 2026-08-22 — fix(shell): impersonation banner unmounts on sign-out + gcTime 0 (SO-4 guard catch, INC-085); test(e2e): A-2 re-anchored to moderators' audit section
- 2026-08-22 — ci: reporter output prettier-exempt (E5 class); fix(ssr): error page logs + DEV-embeds its cause; test(e2e): gotoReady retries the SSR error page once then fails named (INC-085c — flake family unmasked)
- 2026-08-22 — ci(e2e): DEC-018 — CI builds and serves the PRODUCTION bundle (wrangler) in E2E mode; isE2E flag gates test instruments; central document-response guard in e2e/fixtures.ts; reporter quotes [ssr-error] and matches contexts by containment (INC-085d)
- 2026-08-22 — ci: reporter treats zero-test results from failed sources as no-results (webServer deaths now quote their own logs)
- 2026-08-22 — ci(DEC-018): nitro target pinned explicit for the e2e build + build-output verify step (ENOENT wipeout root-caused; INC-085e)
- 2026-08-22 — test(e2e): explicit data-app-ready hydration contract + client-error capture ([client-error] tag, reporter-grepped) — prod-build divergence made self-naming (INC-085f)
- 2026-08-22 — ci: e2e build unminified + full console-arg capture — React #185 will name its component (INC-085g)
- 2026-08-22 — fix(build): e2e build pins cssMinify so the SSR and client graphs hash styles.css identically (404 stylesheet → unstyled e2e pages → strict-mode/#185 failures); fix(shell): useFooterInset stops observing document.body (self-feeding inset loop); test(e2e): console capture reads Error stacks instead of jsonValue (INC-085h)
- 2026-08-22 — fix(shell): name and eliminate useFooterInset's remaining footer-observer feedback loop; test(e2e): retain full pageerror stacks, raise console capture to 2,000 characters, and collapse repeated Client/Server evidence as ×N (INC-085i)
- 2026-08-28 — ci: platform-injected previewAuthStorage.ts takes the Knowledge E5 machine-generated exemption (path-scoped eslint + prettier ignore) — red main from commit 857f049 cleared with no gate weakening (INC-087)
- 2026-08-28 — ci(DEC-019): e2e serve migrated to nitro node-server (workerd refused the build-day compatibility_date); nightly cloudflare parity smoke added; reporter quotes error lines from the full log + final 10 for zero-test sources and flags PLATFORM-ORIGIN? commits (INC-088)
- 2026-08-28 — fix(shell): kill the React #185 ref-callback loop at root — the rail's `CollapsibleTrigger asChild` was handed `WithTooltip` (Fragment/Tooltip Root, neither holds a ref), so Radix's composed state-setter re-dispatched on every render across every mapped row; tooltip now wraps the trigger and returns children unwrapped. Corrects INC-085i's attribution; the shard-1 400 is GoTrue's by-design `invalid_credentials` (INC-089)
- 2026-08-28 — fix(rbac/shell): kill the signed-in React #185 loop at root — `usePermissions` minted a new empty array every render and the shell stored every report as a new object, so the PermissionsLoader effect re-entered forever and the root boundary painted "This page didn't load" (11 of 12 failures in run 33166409697); fix(shell): corrected footer-clamp law — pull-based footer read plus mount/fonts settle passes, scroll/scrollend/resize and the `#main` observer only (INC-090, INC-089 addendum)
  2026-08-28 — fix: track e2e boot-crash fixture by renaming to .log.txt and pinning reporter path (INC-091); add INC-090 addendum
- 2026-08-29 — fix(admin): audit detail expands inline beneath its row (INC-092); impersonation copy states the read-only model; docs: impersonation model & roadmap (DEC-021)
- 2026-08-29 — fix(admin): audit trend as bounded sparkline (≤160px, normalized, sparse labels); test(e2e): audit-expand locators scoped to the visible surface (INC-093)
- 2026-08-29 — test(e2e): auditSurface twin helper — audit locators viewport-correct both ways (INC-084c sixth, INC-093b)
- 2026-08-29 — docs: S31 gate — DEC-017..021 rows, U2b+U3 close-out, harness epoch closed, roadmap reorder (U4 Translations), ACT-U3-1
- 2026-08-29 — ci(DEC-020): CI on dev + fast-forward promote to main on full green; main is the certified record
- 2026-08-29 — ci(DEC-020): all tracking commits target dev; sync-main dispatch = sanctioned divergence repair
- 2026-08-29 — feat(i18n): U4a translations foundation — languages/ui_translations/translator_languages/entity_translations, DEC-017-pattern permissions (step-up, assignable), scoped definer RPCs with placeholder validator + coverage gate, locations name_am backfill (Tier A)
- 2026-08-29 — fix(i18n): matrix vocabulary for the translations registration (coverage guard catch)
- 2026-08-29 — feat(i18n): U4b read seams — `admin_list_languages()` (definer, translations:view) exposes the full roster incl. admin-only languages the RLS policy hides; `get_my_translator_languages()` (auth-callable self-read) makes the strings page honest about scope; proofs P1-P4
- 2026-08-29 — fix(ci): INC-094 declared-mark law — migration marks are read from the file, not assumed equal to the tool-assigned filename stamp; ledger reconciled
- 2026-08-29 — feat(i18n): U4b translations console — languages roster (coverage, admin/public switches, key sync, add-language), per-language strings editor with URL-derived filters and provenance, manage-gated translator scope card on user detail, D3 runtime flip (approved DB bundle merged over the compiled catalog with a logged fallback), TR-1..10 (Tier A)
- 2026-08-29 — fix(i18n): DB bundle overlays the compiled active catalog (chain restored; empty catalog is invisible); test(e2e): TR helper re-anchored to the primitive's real testids (INC-095)
- 2026-08-29 — fix(i18n): am pair for translations.collapse (+parity); test(e2e): expansion scoped via twin helper, scratch-key law, TR-9 anchored (INC-095c-e)
- 2026-08-29 — fix(i18n): publish gate refuses an empty catalog (vacuous-complete hole, TR-6 catch); sheet/dialog close labels i18n'd (D1 on a11y text); sync-first tooltip
- 2026-08-29 — test(e2e): TR-9 verify-or-sync, sized 120s budget, named phases (INC-095i)
- 2026-08-30 — polish(admin): translator card compact empty state + manage-only scope note
- 2026-08-30 — feat(admin): translator card renders only when the target holds a translations:\* permission (effective read via has_permission RPC); no-role targets get one muted line, no controls
- 2026-08-30 — test(e2e): TR-10 proves the conditional translator card, both states (INC-095j)
- 2026-08-30 — fix(rbac): gated definer read for the translator card (invoker-blind probe replaced); errored checks surface, never impersonate absence (INC-095k)
- 2026-08-30 — fix(rbac): translator card reads existing scope (wipe-footgun closed), controls render for eligible targets (streak root), scope RPC + persistence proof (INC-095 l–n)

- 2026-08-30 — feat(translations): AI machine translation via the app-server route (per-row + bulk), revision capture on every writer, CI fake mode (U4c, INC-096)
- 2026-08-30 — fix(api): translate endpoint censused against the installed Start server-route primitive (no separate factory exists; the CI 500s were handler-issued, not SSR renders); every 5xx now logs into [ssr-error] before the structured body (INC-096 b–c)
- 2026-08-30 — fix(api): `has_permission` argument names in `/api/translate` match the SQL declaration (`p_user_id`, `p_resource`, `p_action`); first lit-seam catch (INC-096d)
- 2026-08-30 — test(e2e): TR-11 asserts the full revision pair (capture law); DEC-023 env census reported
- 2026-08-30 — test(e2e): TR-11 orders revisions by the declared changed_at
- 2026-08-30 — fix(i18n): writer order canonicalized — gates before capture before mutate; refused attempts leave no revisions (INC-096f); ci(DEC-023-B): changed-spec fast lane
- 2026-08-30 — test(e2e): scratch keys job-axis complete (run×shard×worker×project) — cross-job collision closed (INC-096f-b)
- 2026-08-30 — test(e2e): per-test scratch identity + self-describing revision dump (INC-096f-c)
- 2026-08-30 — test(e2e): TR-12 asserts per-key DB truth; global-setup reaps stale scratch rows (INC-096g)
- 2026-08-30 — feat(i18n): U4d — entity translations live (categories 97 backfilled, locations adopted, Data console scope, anon bundle + fallback chain); public gate stays ui-scoped (Tier A)
- 2026-08-30 — test(e2e): TR-14 fixture via service client (INC-097b); ci(DEC-024): shards at 2 workers under the identity law, revert-rule armed
- 2026-08-30 — fix(db): U4d-3b — the two service_role grant-sweep migrations recreated after they went missing from the tree; declared marks 20260830100000/20260830110000 preserved (idempotent, ON CONFLICT DO NOTHING)
- 2026-08-30 — ci(DEC-023-B): fast lane made non-gating as ratified
- 2026-08-30 — feat(i18n): U4e — translation History drawer with one-click restore (restore-as-save; every restore is itself history)
- 2026-08-30 — test(e2e): TR-16 joins the self-describing dump law (INC-096f-c)
- 2026-08-30 — test(e2e): fence language for the global bulk + scratch-location fixture for TR-14 — sweep-class collisions closed (INC-097d)
- 2026-08-31 — docs: S32 gate — U4 Translations complete; DEC-022/023/023-B/024; U4 close-out; ACT-U4-1..3; next U5 Locations
- 2026-08-31 — fix(i18n): switcher + runtime honor the publication gate; approve refuses flagged rows (INC-098)
- 2026-08-31 — fix(i18n): non-blocking provider; gated activation reconciles once (INC-098b)
- 2026-08-31 — test(e2e): shell drawer and rail-scroll geometry wait for skeleton count 0 before reading scroll/rects (INC-098c)
- 2026-08-31 — feat(i18n): U4g — approve-all-reviewed (flag-safe, revision-captured), language ordering (switcher follows), orphaned keys excluded from coverage
- 2026-08-31 — fix(db): authenticated EXECUTE restored on translation wrappers with totality proof; TR-21 reads truth from tables (INC-099)
- 2026-08-31 — fix(i18n): deterministic language order (normalized sort, append-on-insert, (sort, code) everywhere); TR-20 self-describing; gitleaks proof-literal allowlisted (INC-099b)
- 2026-08-31 — fix(db): languages_append_sort as INVOKER; migration guard gains a reasoned, printed allowlist (DEC-022-B, INC-099c)
- 2026-08-31 — ci: overwrite artifacts on re-run; report names its attempt (INC-100)
- 2026-08-31 — fix(auth): auth-derived state settles independently of the i18n language read (no Supabase call inside the auth callback); TR-19 gets its own fence, TR-20 names its phases (INC-101)
- 2026-08-31 — fix(auth): session identity applied synchronously from the auth payload; only the profile read hops the auth lock (INC-101 addendum)
- 2026-08-31 — fix(auth/i18n): use-auth restored to last-known-good; provider reads start after auth settles (INC-101b)
- 2026-08-31 — fix(admin): strings page renders from admin sources, never awaits the public gate (INC-102)
- 2026-08-31 — fix(db/admin): admin_list_translations honours an absent orphan filter; roster move controls read one sorted source (INC-103)
- 2026-08-31 — fix(admin): saved marker is page-level state so it survives the approve refetch on both twins; user detail renders its shell before its queries (INC-104)
- 2026-08-31 — fix(i18n): sync orphans only sync-origin keys; TR-20 parks via table write; restore feedback page-level (INC-105)
- 2026-08-31 — fix(admin): reorder controls on mobile; role filter honors URL before options load; test(e2e): TR-21 seeds sync-origin (INC-106)
- 2026-08-31 — fix(admin): one reorder control per twin, in its own twin (INC-106b)
- 2026-08-31 — test(e2e): TR-20 locates row actions per twin and names click/step-up/poll phases (INC-106c)
- 2026-08-31 — fix(ui): roster actions no longer overlap; scroll padding for the fixed band (INC-106d)
- 2026-09-01 — fix(i18n): DB-only languages render (missing compiled layer is empty, not fatal — INC-107); ci: nightly writes its failure evidence (INC-108)
- 2026-09-01 — test(e2e): 5xx URL capture; AU-3 self-describing failure dump (INC-109)
- 2026-09-01 — fix(admin): activity list refreshes on mobile; redirect never awaits i18n (INC-110)
- 2026-09-01 — ci(DEC-019-B): worker compatibility date pinned; nightly cloudflare parity runs for real (INC-111)
- 2026-09-01 — test(e2e): TR-19 phased + strings-page failure dump (INC-112)
- 2026-09-01 — fix(translations): one shared reviewable predicate; gate list never cached (INC-113)
- 2026-09-01 — fix(i18n): gate fetch without filter-shaped busting; failures surface and retry (INC-113b)
- 2026-09-01 — test(e2e): scratch-role reaper; AU-3 audit-row dump (INC-114)
- 2026-09-01 — feat(translations): placeholder-safe MT, one-click restore, honest provenance (INC-115)
- 2026-09-01 — test(e2e): per-project fence languages (INC-115b)
- 2026-09-01 — fix: route-legal fence subtags; profile-edit invalidates activity (INC-115c)
- 2026-09-01 — test(e2e): every user-detail activity assertion dumps detail + audit truth (INC-115d)
- 2026-09-01 — test(e2e): TR-20 desktop-only relative order, TR-20m mobile controls, TR-17 set equality (INC-115e)
- 2026-09-01 — fix(db): audit activity index + sargable predicate (statement timeout, INC-116); test(e2e): TR-22 mobile-visible anchor
- 2026-09-01 — test(e2e): fence-filtered TR-17 set equality, TR-19 seed-before-navigate poll, @global-state quarantine label (INC-117)
- 2026-09-01 — ci(DEC-028): global-state specs serial-only; quarantined failures reported, non-gating. format:check + reporter self-test; CI proves.
- 2026-09-01 — chore(deps): browserslist 4.28.8 + update-browserslist-db 1.3.2 via overrides (whole tree, no nested copy); audit ruling recorded
- 2026-09-01 — ci(DEC-030): retries:1 in the parallel matrix (nightly stays 0); flake ledger rendered, appended and self-tested (INC-118)
- 2026-09-02 — feat(i18n): U4j — data-layer AI (per-row + bulk), data coverage meters, guided language creation from the provider's list with native names, RTL and countries
- 2026-09-02 — fix(ci/e2e): exempt machine-generated flake ledger from prettier (INC-118); TR-24 scopes data-meter locator to language row cell; TR-21 quarantined as @global-state (DEC-028)
- 2026-09-02 — fix(i18n): Data scope enumerates the entity universe — missing translations are untranslated rows, bulk-translatable (walk finding)
- 2026-09-02 — fix(i18n): Data bulk count reads the entity stats correctly (INC-119); TR-24 self-describing
- 2026-09-02 — fix(i18n): Data scope renders untranslated universe rows (composite entity keys); TR-24 waits for a ready count (INC-119b)
- 2026-09-02 — test(e2e): TR-24 anchors its own scratch row via the twin helper; fence entity residue reaped (INC-119c)
- 2026-09-02 — feat(i18n): U4k — Data-scope approve-all + status chips + dual meters (walk findings)
- 2026-09-02 — ci(DEC-029): session injection for non-auth specs, single shared build, six shards — wall-clock roughly halved (revert knob E2E_UI_LOGIN)
- 2026-09-02 — ci(DEC-029-B): revert knob engaged; injection/step-up seam under diagnosis (INC-120); build-artifact + 6 shards retained
- 2026-09-02 — ci(DEC-029-C): session injection live (write-once sentinel, INC-120 closed); credential-lifecycle tests pinned to UI login
- 2026-09-02 — test(e2e): per-user injection sentinel; switchUser clears and asserts identity (INC-120b)
- 2026-09-02 — ci(DEC-029-E): injection parked per pre-committed rule; build-once + six shards retained (INC-120c)
- 2026-09-02 — chore(quality): DEC-025 typed clients + lint · DEC-026 component tests (seed suite + CI job) · DEC-027 spec lint — the pre-push floor
- 2026-09-02 — feat(i18n): U4h — per-device ★ default language with account sync (device wins), dynamic hreflang from the publication gate
- 2026-09-02 — fix(i18n/e2e): U4h star flows converge (INC-121)
- 2026-09-02 — feat(i18n): U4i Tier B — context notes (v2 limitation stated), used-on map + CI freshness guard, advisory length warnings, ETag-cached /api/i18n/:lang, CSV/XLIFF transfer, pseudo-localization in the never-publishable zxa\n
- 2026-09-02 — test(e2e): TR-26 per-key approval truth (J4).
- 2026-09-02 — fix(i18n): U4i walk findings — note render, route-based used-on, amber length chip, idempotent import (no-op on unchanged), pseudo button surfaced (INC-122)
- 2026-09-02 — feat(i18n): U4i-4 — export-all (paged), delete-language (typed confirm, cascading counts, base/public refused), compact switcher (INC-123)
- 2026-09-02 — fix: delete control once per twin; usage map regenerated (guard catch); ACT-U4-7 registered
- 2026-09-02 — fix(i18n): server-side import idempotency (approved never demoted by no-op), step-up always on top, compact switcher rows (INC-124)
- 2026-09-02 — feat(i18n): U4i-7 — undoable imports (batch-tagged, conflict-safe) + final switcher density (INC-125)
- 2026-09-02 — polish(i18n): compact switcher (selection by tint, no check), proportional roster columns, Interface/Content naming with tooltips, one-line actions (walk polish)
- 2026-09-02 — fix(ui): languages roster card twin at mobile, scroll-not-cramp at mid-widths, 360 guard (INC-126)
- 2026-09-02 — docs: S33 gate — translation era closed (U4f–U4k); DEC-019-B..030 recorded; roadmap.md + era handoff imported; next U5 Categories & Locations
  2026-09-02 · S34/D0 · Category Era spec + taxonomy artifacts imported; roadmap, categories.md, system-state, ledger (REQ-017 a–e, DEC-031..033), build-plan banner, ACT backfill.
- 2026-09-03 — feat(db): C1 category era taxonomy migration applied (20260903022519) — 113 active nodes (14 roots, 14 catch-alls), pointer tree rebuilt, jobs retired, am names into entity layer, offer_type at 9 leaves; mark 20260903030000
- 2026-09-03 · C2-mig · category console machinery: exclusions table, cycle trigger, granular categories permissions (manage retired), visibility-law browse RPC + admin RPC set.
- 2026-09-03 · C2-fix · permission-string consumers migrated (sections gate → categories:view) + D2 keys for restructure/assets (EN+AM); usage map regenerated.
- 2026-09-03 · C2-audit-rewrite · INC-129 closed: admin_list_audit bounded (capped total_count, page-scoped profiles join, trigram entity_id search); contract unchanged.
- 2026-09-03 · C2-UI · categories console shipped (roster, create/edit, visibility window, country exclusions, reorder, pointer, retire) + CT-1..CT-7; INC-130 re-land: C7 DataTable contract (cardUntil, min-width, totalLabel) with unit + law L9 proofs; DEC-031 scratch-category reaper.
- 2026-09-03 · C2-UI-FIX · C2b migration (admin_list_categories gains excluded_country_codes; new admin_list_category_pointers) + INC-132 primitive scroller chain + console UX (datetime-local window, icon action row, slug under name, Parent column, root filter, 25/page, pre-ticked exclusions, Browse paths move/remove/add); CT-7b/CT-8/CT-9 and law L10.
- 2026-09-03 — C2-UI-FIX-2. INC-133: the hardcoded-string guard's JSX-text anchor became a closing tag (`</`), ending the `Promise<unknown>` false positive; a two-direction guard-proof fixture pair (`scripts/fixtures/string-scan-{bad,good}`) and CI self-test steps prove it bites and does not over-bite. INC-134: the shared admin content region in `app-shell.tsx` gained `min-w-0 max-w-full` — the first ancestor still at `min-width:auto` above every DataTable scroller (1240×800 census); CT-8 now covers the 1240 band. INC-135: DataTable gained the `wide` column tier (xl-only, card-excluded) and `stickyFirstColumn` (logical `start-0`, `bg-card`), with unit tests and E2E L11. C2c console UX: server-derived slug with read-only preview, `has_image` missing-assets flag + filter, tooltipped badges, active-only parent pickers with paths, parent-aware search, root counts, and a device-persisted page size (CT-10, CT-11). EN+AM keys and usage maps regenerated.
- 2026-09-04 — C2-UI-FIX-3 + C2d. Workflow refrozen (`ci.yml` reverted verbatim to 2ffbaa8) and the hardcoded-string guard now self-tests IN-SCRIPT against its fixture pair (bad ≥1, good 0; mismatch exits 2 with SELF-TEST FAILED). Primitive: `cardUntil` gains `"xl"`, the card twin carries the `wide` tier there, and INC-136's sticky drift is fixed by pinning header AND body cells (selection at `start-0`, first data column at `start-10`). Roster moves to `cardUntil="xl"` without sticky, hides listings/price flags on retired rows, and gains Reactivate (step-up) plus a typed-slug Delete on retired rows only. C2d migration (mark 20260904010000): `expiry_days` optional, `admin_reactivate_category`, `admin_delete_category` with cascade + audit, in-file deny/refusal/cascade proofs. Spec viewport law: every block in `admin-categories.spec.ts` declares its band; CT-8 rewritten (cards at 1024/1240, table at 1440), CT-9 split into 9a/9b, CT-12 reactivate and CT-13 delete walks added; L11 extended to the body cell.
- 2026-09-04 — C2e + UI-FIX-4. C2e migration (mark 20260904020000, INC-137): the definer ACL for `admin_create_category(text, text, text, uuid, boolean)` is restated in one self-describing file (REVOKE FROM PUBLIC, anon; GRANT EXECUTE TO authenticated, service_role) with an in-file privilege proof — no behaviour change. Console converges on the Roles interaction model: the roster row carries exactly ONE verb (Edit, `category-edit-<slug>`), and Visibility · Countries · Browse paths · Move up · Move down · Retire/Reactivate · Delete move into a wrapping full-text verb bar at the top of the editor, testids and gates unchanged. E2E re-anchored: `action()` resolves non-edit verbs inside the open editor, CT-8 becomes the reachability law (360/768/1024/1240 editor verbs reachable with no page or dialog horizontal scroll; 1440 table with an inert scroller), CT-6/CT-12/CT-13 drive their verbs from the bar.
- 2026-09-04 — C2-UI-FIX-5 + C2f + TR-29. Allowlist closes INC-137 (C2c file cites C2e closer 63df0b68; guard green). Roster conforms to the audit table (INC-139): no consumer min-widths, no `cardUntil`, no sticky/wide leftovers — priorities only, Name truncates, and `ROSTER_COLUMN_PRIORITIES` is machine-checked by a unit test. C2f migration (mark 20260904030000, INC-138): pointer edges under retired parents deleted and `admin_list_categories` prefers the root pointer, else the lowest-order ACTIVE parent, contract and ACL restated. TR-29/INC-141: the CSV export reader drops its `orphaned = false` filter (`orphaned: null` = no predicate), so the export row count equals the catalog count. Specs self-diagnose: geometry dumps on CT-8/CT-9a, a lifecycle dump on CT-12, and L10 scrolls the last cell into view before asserting horizontal reachability.
- 2026-09-04 — C2g + UI-FIX-6: catch-all parent law (assert_parent_not_catchall; create/add/move refuse, reorder pins last; mark 20260904040000), live-row editor (INC-142), NULL-honest listing expiry (INC-143), translated server refusals, CT-14.
- 2026-09-04 — UI-FIX-7 (INC-144): the categories verb bar routes every guarded verb (Move up/down, Reactivate) through ONE `runVerb(guard, action)` runner that conforms to the working translations consumers — `guard(...).catch(fail)` — so a step-up refusal or RPC error renders translated in the bar (`category-verb-error`) instead of rejecting an un-awaited promise (F4); the shared post-verification replay in `use-step-up.submitCode` was already correct and is unchanged. Move up/down remain wired to `admin_reorder_categories` (never the pointer RPC); their step-up prompt is server law (`categories:restructure` carries `requires_step_up`) and removing it would need a migration, which this landing does not have. CT-12 rewritten so every wait is a bounded ≤15s poll whose failure carries a freshly computed lifecycle dump; CT-14 resolves the catch-all row through the twin helpers (row + actions region + editor verbs), assertions unchanged.
- 2026-09-04 — C2h + CT-12-TRUTH: `admin_reorder_categories` re-stated on `categories:update` with the step-up call removed; `admin_list_categories.display_order` now reports the resolved parent POINTER edge (the order `get_browse_tree` reads), contract byte-identical; retire dialog hides the reassign picker at zero listings and counts them otherwise (EN+AM keys); CT-12's closing assertion corrected to Active badge + Retire present / Reactivate absent; CT-15 covers reorder with the step-up modal's absence asserted. Migration mark 20260904050000.
- 2026-09-03 · C2i · admin_list_categories row-completeness corrective (board-wide roster regression from C2h read-switch).
- 2026-09-03 · C2j · orphan-visible roster (INC-146): `admin_list_categories` walks only categories that hold a pointer edge and appends every edge-less category as an explicit orphan tail (slug order, NULL parent fields) after the walk; 17-column contract, gate and ACL byte-identical; in-file proofs create and delete a scratch edge-less row to prove the class. Mark 20260904070000.
- 2026-09-03 · DEC-034 · E2E fixture invariant: the categories spec's scratch seeding helper now creates the pointer edge alongside the row (scratch parent or root, next free display_order), so no fixture is an orphan by construction. No spec assertions changed.
- 2026-09-03 · C2k · roster order pinned (INC-147): pointer-edge `display_order` renumbered from a pinned source (roots take the ratified 14-slug sequence, other root edges by slug after 14; every child sibling group dense-renumbered by (display_order, slug) with catch-alls forced last), and `admin_list_categories` now orders by the NUMERIC order-path (root edge order, then each child edge order), ties by slug, orphan tail unchanged. P0 asserts zero colliding sibling groups; P6 STRICT asserts `vehicles` first and the walked root sequence equal to the ratified sequence and to the root-edge display_order. Contract, gate and ACL byte-identical. Mark 20260904080000.
- 2026-09-03 · INC-148 · the categories Move verbs no longer carry a client-side step-up pre-check: reorder is not step-up gated on the server (C2h), so Move calls the mutation directly and the shared `use-step-up` defence-in-depth path still handles any server `step-up required`. Specs gained one `findRow(page, slug)` helper (roster search → twin row) adopted wherever a freshly created or edited scratch row was previously expected on an unsearched page.
- 2026-09-03 · C2-CLOSE · four finals, then the console freezes: (A) CT-12's closing check is search-anchored — the editor is dismissed, `findRow` narrows the roster, and the row reads Active with Retire offered and Reactivate absent; (B) CT-6 seeds one `status:'active'` listing (every required column, service client) BEFORE navigation (J7) and asserts the reassign picker with its "Move 1 listing…" count, the completed reassignment and the listing's new home; (C) the editor's Display-order field is READ-ONLY and bound to the LIVE roster row (Move up/down updates it immediately), leaves the typed update payload, and carries the translated caption "Managed by Move up/down." (EN+AM); (D) INC-150 — the dialog state machine keeps `kind:'edit'` with the secondary surface as its own `sub` axis, so closing Countries/Visibility/Browse paths/Retire/Delete returns to the OPEN editor and only closing the editor returns to the table (CT-16). No migration; step-up top-layer behaviour untouched.
- 2026-09-03 · C2-CT6-IDENTITY (INC-151) · CT-6 now seeds its own reassign target (scratch active category via the existing UI helper, edge included) before navigation, selects the target by identity (`selectOption({ label: targetSlug })`) rather than index or first-match, and asserts the listing lands on the exact target category_id. Both scratch categories are destroyed in `finally`.
- 2026-09-03 · C2-GHOST (INC-152) · (A) structural kill of the ghost editor: the single edit-opener (click AND Enter/Space) is a no-op unless `dialog.kind === "none"` and ≥350ms (`REOPEN_GUARD_MS`) have passed since the last dialog closed, stamped by the one `closeDialog` path every dialog's `onClose` now runs through; (B) confession channel — every categories dialog root carries `data-opened-by` (`row-click` | `keyboard` | `create-button` | `verb-<sub>`), and the specs' `findRow` failure path plus `createViaUi` right after `stepUpIfPrompted` append a `[dialog-dump …]` line listing every open `*-dialog` with its opener (assertions unchanged); (C) the Browse-paths dialog marks the lowest-`display_order` edge with a translated "Primary" chip (EN+AM) whose tooltip reads "Shown as the Parent in the roster and breadcrumbs." No migration, no schema change.
- 2026-09-03 — C2-SETTLE (INC-152 closed, INC-153): re-open guard retuned to the kind-check alone (350ms window and lastClosedAt removed; opener breadcrumbs/dumps stay); CT-6/CT-14 dismiss the returned editor before roster assertions; the shell feed empty-state test scopes its emptiness to a scratch fence category; the CT-15 scratch seeder births rows publicly invisible (visible_until = epoch).
- 2026-09-03 — C5a (AI foundation, mark 20260904090000): `GEMINI_API_KEY` requested as a server-env secret (plus `GEMINI_IMAGE_MODEL` default `gemini-2.5-flash-image`, `GEMINI_TEXT_MODEL` default `gemini-2.5-flash-lite`, `GEMINI_FAKE` for CI); storage bucket `category-assets` created PRIVATE (workspace policy blocks public buckets — operator must flip it) with anon/authenticated read and service-role-only write policies plus in-file proofs; new pipeline module `src/server/category-images/**` (pngjs, pure JS — `@cf-wasm/photon` measured at 6.1 MB wasm and rejected under G2) implementing the master prompt verbatim, decode → white-to-transparent → content-bounds crop → 85% fill scale → six diagonal `ethio.com` watermarks at −30°/28% BEHIND the icon → 512 card, 128 thumb, programmatic 1200×630 OG, all stages timed; routes `POST /api/admin/categories/generate-image` (+ `GET ?probe=1` A7 walk page) and `POST /api/admin/categories/suggest-icon`, both on the censused U4c gate checking `categories:assets`, with `[ssr-error]` logging and honest 402/429 pass-through; server-side lucide allowlist (none existed) with `Package` fallback; `e2e/category-image-routes.spec.ts` (CI-1..CI-3, fake mode). No console UI (C5b).
- 2026-09-03 — C5a-FIX (keyless ⇒ fake): mode precedence decided in one place in `src/server/category-images/gemini.ts` — `GEMINI_FAKE=1` ⇒ fake, else key present ⇒ real, else ⇒ fake with a single `[ssr-error] category-images: no GEMINI_API_KEY — fake mode` warn at first use, so no route can 500 on a missing key; both routes' catch blocks now log the true error with a stable code (`image_generate_failed` / `icon_suggest_failed`) on the `[ssr-error]` channel before the unchanged generic response body (F4); i18n usage map regenerated (both artifacts). No migration, no schema change.
- 2026-09-03 — C5a-2 (INC-154): category image uploads run in CALLER CONTEXT. Migration `20260903114705_81fd36ba-...` (self-mark `20260904100000`) asserts the `category-assets` bucket is public (SQL writes to `storage.buckets` are platform-rejected and the storage tool is blocked by `public_buckets_blocked`, so the operator's manual flip is asserted, not reproduced), adds `authenticated` INSERT/UPDATE/DELETE policies on `storage.objects` gated by `has_permission(auth.uid(),'categories','assets')`, and adds the gated definer RPC `admin_set_category_images(uuid,text,text,text,text)` (F5 order, audited `category.set_images`, four columns only, REVOKE/GRANT restated in-file). In-file proofs P1 (bucket insert refused 42501 without the permission), P2 (RPC deny-case), P3 (happy-path read-back on a scratch row) all passed. `/api/admin/categories/generate-image` now uploads and persists through the gate's caller-context client; no service-role client remains under `src/server/category-images` or the category API routes.
- 2026-09-03 — C5a-3: stage-honest category-image probe. Model defaults baked to `gemini-3.5-flash-lite` / `gemini-3.1-flash-image` (env still overrides); pipeline stages (model-call · decode · process · watermark · encode · upload · persist) tag every throw with `{stage}`, logged on `[ssr-error]` and echoed in the admin-gated probe response only (non-probe POST keeps the generic body); format tolerance decodes PNG/JPEG by sniffed magic bytes (`jpeg-js` added, pure JS) and reports WebP honestly rather than adding a wasm decoder; unknown or RLS-hidden `categoryId` now answers 404 `category not found` on both probe and POST. Files: src/server/category-images/{gemini,pipeline,raster}.ts · src/routes/api/admin/categories/{generate-image,suggest-icon}.ts · e2e/category-image-routes.spec.ts.
- C5b — category imagery everywhere: Image tab (generate/regenerate, custom prompt, three-asset preview + timings), create-flow AI icon suggestion with manual fallback and a generate-now step, bulk "Generate missing (max 25)" serial fill with per-row stage-tagged failures; CI-4/CI-5/CT-17; EN+AM keys; docs/features/categories.md updated.
- 2026-09-03 — C5b-HELPER (INC-155): `createViaUi` teaches the Generate-now step — bounded wait for `category-create-generate-step`, clicks `category-create-generate-skip`, asserts the create dialog count is 0, then searches for the row; CT-17 remains the only spec that clicks Generate.
- 2026-09-03 — CI5-SCOPE (INC-153, second occurrence): census verdict — the bulk fill already derives its candidates from `filtered` (the visible filter+search set) capped at `BULK_LIMIT` 25, so `categories-page.tsx` is unchanged. CI-5 now scopes its world: it enables the Missing filter and searches its worker-scratch prefix so exactly its three seeded rows are visible, asserts the visible set is 3, runs the bulk, captures the transient `3/3` progress caption, and polls (≤15s, standing dump of progress/summary/per-row DB truth) for the `3 generated · 0 failed` summary and every row's `image_url`. Files: e2e/admin-categories.spec.ts.
- 2026-09-03 · CI5-IDS · CI-5 conformed to the console's real testids (missing filter, bulk generate/progress/summary, search, page size); pre-poll interactions bounded ≤15s with the standing bulk dump; the search prefix is now derived from the seeder's returned slugs. Files: e2e/admin-categories.spec.ts.
- 2026-09-03 · C5c · CI-5 declares its budget (`test.setTimeout(120_000)`, workload math in comment) and its dumps became lazy (built only inside the failure path via `lazily()`); art direction: OG icon now fills 75% of the shorter dimension (`OG_FILL_RATIO`), watermark cut from six marks at 0.28 to three at 0.10, card 512 unchanged at the 85% law, thumb unchanged, fake fixture unaffected (it bakes no watermark/OG params); uniform prompt: the custom-prompt textarea and its EN/AM keys removed, the route still accepts `customPrompt` but persists `image_generation_prompt` only for a genuinely custom prompt (NULL today), CI-4 re-anchored to assert NULL after a house generation. Files: e2e/admin-categories.spec.ts · src/server/category-images/{pipeline.ts,prompt.ts} · src/features/admin-categories/category-image-dialog.tsx · src/routes/api/admin/categories/generate-image.ts · locales en+am · usage maps · docs/features/categories.md.
- 2026-09-03 · C5e · Pipeline stage regression closed (the four stages record themselves, the pipeline refuses <4, `processMs` floored at 1ms because the Worker clock is frozen across pure CPU work; CI-2/CI-4 assert `processMs > 0`); versioned assets `<id>/card-<genTs>.png` (thumb/og alike) with the three persisted URLs re-versioned per generation and prior objects pruned best-effort after persist (failure logged `[ssr-error] … image_prune_failed`); the image dialog renders the URLs the generation returned instead of guessing deterministic ones; CI-2's prompt assert flipped to NULL and its cleanup lists the prefix; CI-4 now asserts all three URLs CHANGE on regenerate; create flow silenced (no icon text/Change/picker, suggestion applied silently, `Package` fallback silent) and the create action stopped swallowing step-up refusals so the Generate-now step follows every successful create; CT-17 realigned; CI-5 gained a labelled breadcrumb trail and a 110s watchdog that names the hung line. Files: src/server/category-images/pipeline.ts · src/routes/api/admin/categories/generate-image.ts · src/features/admin-categories/{category-dialogs.tsx,category-image-dialog.tsx} · e2e/{category-image-routes,admin-categories}.spec.ts · locales en+am · usage maps · docs/features/categories.md.
- 2026-09-04 · C5f · Service hardening: `generateCategoryImage` now runs under a 30s `AbortController` budget; an abort throws `CategoryImageError` with stage `client-timeout` (status 0, key-safe message), which the bulk loop's existing catch surfaces as a stage-labelled failure and advances past — the server-side generation is not cancelled, so db-truth may still show the row filled. CI-5's dump hole closed: `bulkDump` now includes the `category-bulk-failures` text alongside progress, summary and per-slug `image_url` DB truth, and the transient-progress assert runs inside `lazily()` so it fails with the full dump and trail. Files: src/features/admin-categories/category-images-service.ts · e2e/admin-categories.spec.ts.
- 2026-09-04 · C5g (INC-155) · OG geometry: icon scaled to 0.88 of the 630px canvas height (554.4px, was 0.75 → 472.5px; card 0.85 of 512 and the derived 128 thumb unchanged). Colour law added verbatim to the house prompt (green dominant, gold ONE accent ≤~15% of the inked area, never alternating, never split). ACCEPT landed: migration `20260904050152_6b6fc9fb-cdaf-4cfc-ad48-92bfb9e15913.sql` (self-mark 20260904110000) adds `categories.image_accepted_at` and the gated/step-up-aware/audited definer `admin_accept_category_image(p_id)` (REVOKE/GRANT in-file; refuses an imageless category with `admin.categories.error.acceptNoImage`), and `admin_set_category_images` now clears the stamp on every persist so a regeneration un-accepts; the image dialog gained Accept/Re-accept plus a dated Accepted badge. Create flow: the Generate-now step is gone (keys removed) — a successful create closes the create dialog, shows an inline "Category created." status and opens the FULL editor on the new row with the Image surface in front; `createViaUi` now dismisses image+editor before touching the roster and CT-17 re-anchors on create → editor/Image → Generate → assets. CI-4 asserts accept stamps the row and a regeneration clears it; CI-2 asserts a persist never lands pre-accepted; CI-5 is tagged `@global-state` (INC-117 quarantine mechanism, non-gating, still reported) and its 110s watchdog is now raced around the ENTIRE awaited chain. Files: supabase/migrations/20260904050152_6b6fc9fb-cdaf-4cfc-ad48-92bfb9e15913.sql · src/server/category-images/{pipeline.ts,prompt.ts} · src/features/admin-categories/{category-dialogs.tsx,category-image-dialog.tsx,category-images-service.ts,categories-page.tsx} · e2e/{admin-categories,category-image-routes}.spec.ts · locales en+am · usage maps · docs/features/categories.md.
- 2026-09-04 — C5h: migration 20260904053127 (mark 20260904120000) adds gated definer `admin_get_category_images` (deny + shape + truth + empty-set proofs); the Image dialog now reads stored truth on open (translated loading state), renders saved assets like fresh ones and shows the Accepted badge from `image_accepted_at`; button law — no Accept without imagery, Accept only while unaccepted, accept confirms then CLOSES the dialog; `reaccept` key retired; CI-4b (route spec) proves generate → accept → reader returns stored URLs + stamp.
- 2026-09-04 — C5i: DEC-035: audit gate retry wrapper (G22 named change) — `scripts/audit-gate.sh` runs `bun audit --audit-level=high` with 3 attempts × 20s backoff, exits 1 fail-closed with the same message only after the third unreachable, and fails immediately (never retried) on a real high/critical finding; `ci.yml`'s audit step is now the single line invoking it. Icons made visible: the roster Name cell renders the row's lucide glyph before the name in both twins (`categoryGlyph`, Package fallback), the edit dialog shows a live glyph preview (`category-edit-icon-preview`) beside the Icon input, and the PUBLIC marketplace rail now renders the STORED `categories.icon` glyph (slug map from src/config/panels.ts kept as fallback, `Package` for unknown names) — the rail's read (`use-feed.ts`) gained the `icon` column to make that possible. CI-4 re-anchored to the shipped flow: generate → assets → Accept → dialog CLOSES → reopen → stored assets + `category-image-accepted-badge` (no Accept button) → Regenerate → badge gone, Accept returns. Files: scripts/audit-gate.sh · .github/workflows/ci.yml · src/components/shell/app-rail.tsx · src/features/feed/use-feed.ts · src/features/admin-categories/{categories-page.tsx,category-dialogs.tsx} · e2e/admin-categories.spec.ts.
- 2026-09-05 — C5j: usage maps regenerate on every src-touching landing — guard class, third strike.
- 2026-09-05 — C5k: INLINE CREATE. Migration 20260905082325 (self-mark 20260905082325) replaces `admin_create_category` with a nine-parameter contract — `p_price_enabled boolean DEFAULT true`, `p_expiry_days integer DEFAULT NULL`, `p_visible_from timestamptz DEFAULT NULL`, `p_visible_until timestamptz DEFAULT NULL` added after the existing five; slug derivation, catch-all parent refusal, pointer-edge creation, audit capture and the gate order are byte-for-byte unchanged, REVOKE/GRANT restated in-file, proofs cover a defaults-only call (matches today's row) and a full-params read-back (proof rows deleted; their audit lines stay, the log is append-only by law). UI: the create and edit dialogs now consume ONE shared `CategoryFormFields` component (name, slug preview, silent icon suggestion with live glyph preview + Change, parent picker, expiry with the "No expiry" placeholder, accepts-listings, price-enabled, and — create only — the visibility window plus a translated "Country visibility is set after creation." caption); create's success still opens the full editor on the new row with Image in front. `createViaUi` is unchanged (every new field is optional); CT-17 extends with DB truth for price-off + expiry 45 + a future window, and its icon assertion follows the shared form (suggestion text still silent, Change control now present). Keys `admin.categories.field.iconChange` and `admin.categories.create.countriesLater` (EN+AM), usage maps regenerated. Files: supabase/migrations/20260905082325_2fa932b1-00ba-4549-bfb2-a56ffcf43549.sql · src/features/admin-categories/{category-form-fields.tsx,category-dialogs.tsx,categories-service.ts} · e2e/admin-categories.spec.ts · locales en+am · usage maps · docs/features/categories.md.
- 2026-09-05 — C5l: CREATE, COMPLETE — two-step inline create. One dialog, two steps: Step 1 "Details" carries the full shared field set + inline Countries multi-select + Position select ("At the end" default, or before a chosen ACTIVE sibling — catch-alls excluded); Save creates via `admin_create_category` then CHAINS `admin_set_country_exclusions` and `admin_reorder_categories` (no RPC change — the create RPC appends, placement is a post-create reorder; census accepted). Step 2 "Image" is the shared `CategoryImagePanel` extracted from the editor's Image dialog (no fork — B3); Generate/Accept live there, Finish closes. The page's C5g editor auto-open handoff and its `create.created` notice are removed (scope-widened per ruling). Partial-failure law: a chained step failing surfaces the translated refusal on Step 2 — the created row is never lost. Icon flow: empty until named (translated hint), silent blur suggestion fills it, Package applied only at save time. Root-filter counts and Position options are ACTIVE-only. `createViaUi` re-anchored (Step 2 → Finish → findRow); CT-17 rewritten as the full-field two-step spec with DB truth for the exclusion row and the between-siblings edge order. Keys: `create.stepDetails/stepImage/finish/position/positionEnd/countries/countriesHint`, `field.iconHint`, `error.createPartial` (EN+AM); `create.created`/`create.countriesLater` retired; usage maps regenerated (631 keys). Files: src/features/admin-categories/{category-dialogs.tsx,category-form-fields.tsx,category-image-dialog.tsx,categories-page.tsx} · e2e/admin-categories.spec.ts · locales en+am · usage maps · docs/features/categories.md.
- 2026-09-05 — NIGHTLY-HYGIENE: MF-2 neutralizes the step-up freshness stamp before the gate leg; RP-1 asserts and self-heals the moderator grant baseline via the service client; CI-5 and TR-30 carry the " @global-state" title suffix so the nightly gating count reconciles to genuine failures.
- 2026-09-05 — C5m: one CategoryEditorDialog with create/edit modes replaces the standalone create dialog (in-place transition to the new id with the shared image surface in front); Position options read the active non-catch-all children of the selected parent with an N+1 end option and a count caption; createViaUi and CT-17 shrink onto the shared editor assertions.
  2026-09-05 — e2e: STAB-1 + DEC-036 — CT-17 pre-Save interactions fully bounded (poll+dump per control); reaper window 24h→3h; global-setup staging maintenance sweep (7d e2e-actor audit rows, 3h e2e category-assets objects) with logged counts
- 2026-09-05 — CT17-OPTIONS: census verdict — a pointer-less scratch ROOT lands in the roster's orphan tail, so its edged children are unreachable from the walk and the position select renders zero siblings; the CT-17 seeder now gives every seeded root its NULL-parent pointer row, and the position step's failure dump prints every rendered option label, service-client truth for both siblings (id · parent edges · is_active · is_catchall) and the form's selected parentId.
- 2026-09-05 — INC-158: the DEC-036 maintenance sweep carries a non-fatal ratified-AM tripwire — active non-e2e categories lacking an approved Amharic name log `[e2e:maintenance] RATIFIED-AM GAP: <n> rows: <slugs>`, otherwise `[e2e:maintenance] ratified-am intact`.
- 2026-09-05 — D-GATE · Category Era governance close (S34–C5 ledger entry, INC-127..158, DEC-031..036, handoff)
- 2026-09-06 — L1+L2 · DEC-037 (fixes INC-159): the two heaviest specs split three ways for shard balance — `admin-translations.spec.ts` → console/data/governance, `admin-categories.spec.ts` → console/lifecycle/images, with every title, tag, fence and seeded identity byte-identical and the shared helpers extracted to `e2e/helpers/translations.ts` and `e2e/helpers/categories.ts`; the audit maintenance door (`maintenance_prune_audit`, service_role only, bounded to actor list AND cutoff) replaces the DELETE the append-only trigger had always refused, and global-setup's audit window narrows from 7 days to 24 hours and logs the returned count.
- 2026-09-06 — L2b · orphan-actor audit prune door (DEC-037 completion) + [intentional-delete] marker for the L1 split: `maintenance_prune_audit_orphans(p_cutoff)` removes stale audit rows whose actor no longer exists in auth.users (the half of DEC-037 the named-actor door cannot reach), writes the single `audit.maintenance_prune` summary row, service_role-only with the same ethio.audit_maintenance GUC latch; in-file proofs cover orphan prune+count, authenticated refusal, plain-DELETE refusal and GUC non-leakage, leaving zero residue.
- 2026-09-06 — L1c · cleanup co-location audit (survivor class from the L1 split): the two helper-owned `signInAsSuperAdmin` mints in `e2e/helpers/categories.ts` and `e2e/helpers/translations.ts` now register the minted super-admin on the line after the mint and dispose it in a helper-owned `afterEach`, so the disposal travels with the minting site instead of relying on the process-wide teardown in a sibling file; no assertion, title, tag or identity changed (J1), and global-setup/teardown are untouched.
- 2026-09-06 — C3a · attributes completion, brand-as-attribute, and the Services-edge flip (C-era spec §4): the donor attribute definitions are imported onto their surviving C1 targets at the narrowest truthful node (types mapped 1:1, `select`→`single_select`, options conformed to `{value,label_en,label_am:null}`, donor filterable/searchable flags carried), with `ON CONFLICT (category_id, attr_key) DO NOTHING` and a per-row checksum proof that the 63 pre-existing rows are preserved byte-identical (63 → 225 across 66 categories); Make lands as an attribute on Cars with its full 109-option list (brand = attribute, never an entity); `is_filterable`/`is_searchable` are added `IF NOT EXISTS` (already present in prod, so a no-op safeguard); and the Services edge of `auto-services`, `realtor-services` and `fitness-centers` is swapped below its goods-root edge so the roster's own parent-pick rule resolves all three to Services. In-file proofs cover type-map totality, target liveness, options shape, preserved-row integrity, the cars.make option count, the three flip resolutions and the catch-all inheritance walk. Self-mark `20260906060000`.
- 2026-09-06 — C3b · normalized attribute model (definitions + links; dedup by construction): `public.attributes` becomes the definition library (`attr_key` unique, type CHECK over the existing seven-type set, options, help text) and `public.category_attribute_links` carries the per-category posture (required/filterable/searchable, display_order, `card_rank` with a DEFERRABLE unique per category), both RLS-enabled with public read + client-write denial and explicit GRANTs incl. service_role, indexed on category and attribute. The 225 `category_attributes` rows backfill to 205 definitions + 225 links: identical `(attr_key, attr_type, options)` collapse to one definition, and the 26 keys whose options differ across categories get 79 per-variant definitions keyed `attr_key || '-' || <lowest category slug in the group>` (groups are disjoint, so derived keys are unique by construction) — nothing is merged blindly. `category_attributes` is NOT dropped: it stays the read fallback until the C3c cutover (named deferral, closed at the era gate). Six audited definer RPCs land with in-file deny proofs: `admin_list_attributes` (categories:view, usage counts, index-backed), `admin_upsert_attribute` and `admin_link_attribute`/`admin_unlink_attribute` (categories:update, translated key-collision refusal), `admin_delete_attribute` (categories:restructure + step-up, refuses while links exist and returns the count — blast-radius law), and `admin_set_card_attributes` (ranks 1..n, refuses more than three). Self-mark `20260906070000`.
- 2026-09-06 — CT6-ESCAPE (INC-160): CT-6's post-retire editor dismissal in `e2e/admin-categories-console.spec.ts` is now bounded — one Escape + 5 s poll, a `dialogDump` into the trail if the dialog persists, a second Escape + 10 s poll, and a thrown failure carrying the full dump; `test.step` labels record whether press 1 or press 2 closed the dialog.
- 2026-09-06 — C3c · attribute library, link manager, card picker and the primary/secondary parent cell: `/admin/attributes` replaces the U6 placeholder with the definitions roster (C7 DataTable defaults, search, usage counts) plus the three write doors — create/edit a definition, delete with the blast-radius count and typed key, and MERGE (`admin_merge_attributes`, categories:restructure + step-up) which repoints every source link onto the survivor, folds sibling links inside one category keeping the stricter required/filterable posture, and removes the sources; `admin_list_categories` is widened by `attribute_count`, `card_attribute_count` and `secondary_parent_names` behind an in-file parity proof (17 old columns and the row ordering byte-identical), and `admin_list_category_attribute_links` lands as the gated read for the per-category manager. The category editor gains the Attributes verb (link/unlink, required + filterable toggles, Move up/down, searchable picker, card picker for up to three ranked attributes) and the roster gains the amber "Needs card attributes" flag for an active listing-accepting category below two card attributes, plus the Parent cell's "Primary" chip and "Also in: …" line. Self-mark `20260906090000`. SCOPE NOTE: Part C's Move up/down had no landed writer, so a SECOND migration adds `admin_set_attribute_link_order` (categories:update + step-up, whole-set totality check, audited, service-parity ACL) with in-file refusal/no-trace proofs; self-mark `20260906091000`. New E2E `e2e/admin-attributes.spec.ts` (AT-1..AT-6) and CT-9a/9b parent-cell asserts read pointer DB truth.
- 2026-09-06 — C3d · roster RPC performance (INC-161) + first-run spec conformance: `admin_list_categories` keeps its 20-column contract byte-identical but now computes `attribute_count`/`card_attribute_count` from ONE `GROUP BY` over `category_attribute_links`, the secondary browse parents from ONE `GROUP BY` over `category_tree_pointers` joined once to the walk, and the exclusion/listing aggregates from their own single-pass CTEs — no correlated per-row subqueries remain, which is what drove the 57014 timeouts. In-file proofs: full-contract parity (rows, ordering, all 20 columns via `EXCEPT ALL`) and an `EXPLAIN (ANALYZE, BUFFERS)` raised as NOTICE with an assertion that the links table is scanned exactly once and an aggregate node survives planning (top node `cost=927.61..928.48`, 154 rows, ~15 ms). Self-mark `20260906100000`. Specs: AT-2 now uses the live `attributes_attr_type_check` vocabulary (`single_select`, never `select`); AT-2/AT-5 and CT-9a/9b resolve every per-row element inside its own row through twin-aware helpers (J5), with bounded `test.step` phases and `dialogDump` diagnostics on every new poll.
- 2026-09-06 — C3e · type vocabulary + blast-radius rendering: the attribute client seam now speaks the live `attributes_attr_type_check` set (`text · number · single_select · multi_select · boolean · date · range`; options-bearing = `single_select`/`multi_select`), retiring the phantom `select` value the editor offered but the CHECK refused; type label keys for `single_select`/`date`/`range` added EN+AM and the usage map regenerated. The delete dialog parses the server refusal `admin.attributes.error.deleteHasLinks:<n>` and re-states the blast-radius line (testid `attribute-delete-blast`) with the SERVER's count instead of a generic error line; the key ships EN+AM with a `{count}` slot. AT-5 asserts exactly that contract (blast line carries `1`, no `attribute-dialog-error`).
- 2026-09-06 — AT5-ONE-MESSAGE: the delete-attribute dialog clears the generic error state (the `attribute-dialog-error` node is removed from the DOM, not hidden or emptied) whenever the server returns `admin.attributes.error.deleteHasLinks:<n>`, so only the amber blast-radius line renders with the server's count; all other failures keep the existing generic path (F4).
- 2026-09-06 — AT5-RENDER-GUARD + CT8-TOUCH: the delete-attribute dialog now renders its generic error node only while no blast-radius count owns the message slot (and clears that count before unrelated failures), making the one-line refusal invariant resilient to later error-state writes; every category editor verb-bar button now carries an explicit 44px minimum height at every viewport, closing INC-162's measured 42.70px touch-target shortfall.
- 2026-09-06 — AT5-DUCK: `useAttributeError` and `failDelete` in `src/features/admin-attributes/attribute-dialogs.tsx` now read the thrown object's `.message` through a duck-typed check (`typeof ...message === "string"`) instead of `instanceof Error`, because Supabase/PostgREST throws `PostgrestError` objects that do not inherit from `Error`; the existing `deleteHasLinks:<n>` regex now matches at runtime and the render guard shows exactly one blast line.
- 2026-09-06 — C3f: AT-5 and AT-6 in `e2e/admin-attributes.spec.ts` declare an explicit 120s budget with a workload comment (super-admin sign-in + category minted through the categories console + a hop to the attributes library + step-up-capable submissions), and their inter-surface navigation is wrapped in a bounded `test.step` that asserts the library rendered with a `dialogDump` message; no assertion meanings changed. Part B (INC-163 redirect-latency census) was report-only — no source changes.
- 2026-09-06 — STAB-I18N (INC-164): `/api/i18n/:lang` now serves each language's approved bundle from an in-process cache keyed `(lang, publication_version)` with a 15s version TTL — inside the window a request makes no database call at all, after it one tiny version read, and only a changed version rebuilds the bundle. ETag remains the publication version (strong validator) and `If-None-Match` still answers `304`. Census found the invalidation key already correct (`get_ui_bundle_version` hashes `max(updated_at)+count` over exactly the bundle rows, and an unpublished language hashes to `|empty`) and the covering index `ui_translations_lang_status_idx (lang_code, status)` already present, so no migration was needed. New API-level spec `e2e/i18n-bundle.spec.ts` (IB-1 identical/validated/304, IB-2 publish moves version + bundle); existing i18n specs untouched.
- 2026-09-06 — STAB-SETUP: DEC-036 amendment — single-owner maintenance. `e2e/global-setup.ts` now runs the reaper sections (revisions, translations, fences, entity translations, locations, roles, categories), the DEC-036 audit prune, the storage sweep and the ratified-am tripwire ONLY in the owning job (`E2E_SHARD === "1"`, and unsharded local runs which have no herd); every other shard logs `[e2e:setup] maintenance skipped (owner: shard 1)` and proceeds directly to its own user minting, ending the t=0 thundering herd on staging. The teardown is untouched.
- 2026-09-06 — STAB-H (DEC-039; INC-165; INC-153 second instance): migration `20260906204408_9956d40a-6937-449e-b1d8-4f170a63271e.sql` sets `statement_timeout = '30s'` on the `authenticated` and `anon` roles with an in-file read-back of both (self-mark `20260906210000`), so a queued read tolerates a burst instead of dying at 57014. Listing hygiene: `seedActiveListing()` titles now carry the scratch prefix (`e2e-cat-listing-…`) and the OWNER setup reaps listings older than the 3h cutoff whose seller is an e2e user OR whose title carries that prefix, logging the count. `e2e/shell.spec.ts`'s gutter test no longer assumes bare "/" is empty — it fences itself on a scratch category exactly like its sibling empty-state test. Server-side Supabase calls exceeding 5000 ms now log `[slow-rpc] <function or table> <ms>`; no behaviour change.
- 2026-09-06 — L4 (DEC-038): job-scoped identity pool. `e2e/global-setup.ts` mints ONE super admin per job (both the maintenance owner and every other shard), grants `super_admin`, enrols and verifies a single TOTP factor through the GoTrue MFA endpoints the app's own client uses, and persists `{id, email, password, displayName, secret, factorId}` in the state file under `superAdmin` (identity from `mintEmail`, so the teardown's ownership filter already reaps it). New `useJobSuperAdmin(page)` in `e2e/helpers/ui.ts` signs that identity in — injected path elevates the grant to AAL2 in node with the pooled factor (AAL parity with the old `enrollAndStepUp`, step-up hint deliberately unwritten so gates still prompt and `stepUpIfPrompted` still answers), `E2E_UI_LOGIN=1` falls back to the real UI door. `signInAsSuperAdmin` in `e2e/helpers/{categories,translations}.ts` and the file-local copies in `e2e/admin-roles.spec.ts` / `e2e/admin-audit.spec.ts` now delegate to it; their mints (and the L1c disposers that reaped them) are gone. Enrolment sites drop from 21 call paths to the enrolment/step-up-focused ones only. No title, tag, identity-naming or assertion changes.
- 2026-09-06 — L4b (DEC-038 repair; DECLARED IDENTITY LAW): the pool contract is now asserted, not assumed. `e2e/global-setup.ts` reads the pooled user's factors back through the admin API after challenge+verify and throws with the full factor list unless exactly one TOTP factor exists with status `verified` — an unverified pool is never written to the state file. `useJobSuperAdmin` (e2e/helpers/ui.ts) elevates IN THE BROWSER through the app's own client (`mfa.challengeAndVerify` on the pooled factor, retried across TOTP windows) and reads `getAuthenticatorAssuranceLevel().currentLevel === "aal2"` back before returning, restoring the exact state the old enrol-in-session helper left for direct-RPC tests (RP-4, TR-6); `stepUpIfPrompted` remains the answerer on freshness re-prompts. On acquire it also ends any still-active impersonation session for the pooled identity via `get_active_impersonation` + `end_impersonation(p_session)` and logs when it did. DECLARED IDENTITY LAW: tests that mutate their own auth state carry the Playwright tag `@private-identity` (IMP-1/2/3, RP-6, MF-1/2/5/6/7); `useJobSuperAdmin` reads `test.info().tags` and hands a tagged test a private mint (`mintPrivateSuperAdmin`, the pre-L4 path) instead of the pool. New `scripts/check-identity-tags.sh` enforces the law statically (impersonation start/end, TOTP enrol/unenrol, admin factor APIs) with a `--self-test` fixture proof; the four out-of-scope sites that mint their own identity today (CT-7, TR-9, AU-3/4/5/9/10) are named as deferred, never hidden. No title, assertion or identity-naming changes.
  2026-09-06 — test: L4c — elevation race fixed (session poll before challengeAndVerify); TR-7/11/13/16/23 carry route status+body, pooled user id, DB row, and translator-scope evidence (no assertion changes)
- 2026-09-07 · L5 · DEC-040 token-derived step-up freshness (no localStorage hint), DEC-041 guard completion contract + pooled node AAL2 session reuse; TR-7b regression (INC-166).
  2026-09-07 — e2e: L5b pool path injects the node AAL2 session unconditionally (DEC-041 live); elevateInBrowser now belongs to mintPrivateSuperAdmin alone. session: retired the U1f stepped-up-at hint (INC-167) — freshness is JWT-derived; legacy key shed on sign-out. DEC-023 census re-run recorded.
  2026-09-07 — e2e: L5c (DEC-041 amended) IDENTITY PER WORKER, SESSION PER TEST. `global-setup` mints one pre-enrolled super admin PER WORKER SLOT (`E2E_WORKERS`, default 2), each with its own verified TOTP factor and secret, written as the `superAdmins[]` array (job × worker namespaced, reaped by teardown's existing `+${PROCESS_ID}-` ownership filter); no session is persisted any more — the verify response only proves AAL2 and is discarded. `useJobSuperAdmin` selects `superAdmins[test.info().parallelIndex]` and, in node, does password grant → per-attempt challenge → TOTP verify → assert `aal` = aal2 → inject THAT session, then asserts identity, header and AAL2 from the client. Fixes INC-168 (a shared session revoked out from under concurrent workers) and INC-169 (a stored challenge/session aged past its window → "verification expired"); direct-RPC step-up tests (RP-4, TR-6) are fresh by construction.
  2026-09-07 — ui: L6 TOUCH SIZE AT THE PRIMITIVE (C2 law) + RATIFIED PUBLIC ANCHORS (INC-170). `Button` gains `size="touch"` (`h-11 min-h-11 px-4 py-2`); the admin-categories verb bar, dialog footers, create/toolbar buttons and the attribute-dialog footers use it — the dead per-instance `min-h-[44px]` hack at `categories-page.tsx` (CT-8 verb helper) is gone. INC-170: `firstCategory` in `e2e/category-nav.spec.ts` now anchors structurally, excluding `rail-category-e2e-cat-` scratch rows (INC-153/DEC-031 lineage) so a graveyard row can never become the "first category"; C-1/C-2 assertions unchanged.
- C3-UX-1 — attribute library on the DataTable primitive (cardUntil=lg, primitive-owned min-widths, options expansion), URL-backed `?category=` filter, and Assign-to-category row action; AT-7/AT-8/AT-9 added; INC-171 rider polls the rail guard so C-1..C-3 run or skip together.
- 2026-09-07 — C3-UX-1b: PRIMITIVE-OWNED PAGINATION (INC-172) — `DataTable` accepts `page` + `pageSize` and slices `rows` itself; `categories-page` and `attributes-page` pass the full filtered set and drop their local `.slice()`. INC-170b: `firstCategory` excludes the WHOLE `rail-category-e2e-` family (census: every scratch generator already begins with `e2e-`, so no helper was renamed). DEC-023: `bun run e2e:local` builds with the CI E2E env (staging `VITE_SUPABASE_*`) and serves that build for Playwright; documented in the harness doc as the pre-commit command (it also unsets `LOVABLE_SANDBOX`/`DEV_SERVER__PROJECT_PATH`, which force the cloudflare preset inside the editor sandbox and made the node serve 404 every client asset). AT-9's row locator now excludes the `-expanded-row` expansion `<tr>`, so the 25-row page window is counted as data rows.
- 2026-09-07 — C3-UX-1c: ROW ACTIONS MENU + USED-BY CHIPS + REMOVE FROM CATEGORY. New gated read `admin_list_attribute_categories` (`categories:view`, SECURITY DEFINER, authenticated-only EXECUTE, mark 20260907050000) names the categories each attribute is linked to and carries the link id — `admin_list_attributes` returns a bare count, so the library could neither name a category nor address a link row. The library's actions column is now ONE ⋯ menu (Edit · Assign · Remove from category · Delete) instead of stacked verbs; Used by renders category-name chips with the count moved into the card twin's caption; Remove from category writes through the existing `admin_unlink_attribute` door and names the category in its confirmation. `category-attributes-dialog` verbs move from the `min-h-11` class hack to `size="touch"`. INC-173: `useJobSuperAdmin` resets the pooled identity's `preferred_language` to `en` in node, before injection, through `user_set_preferred_language` (the switcher's own door), logging when it changed something. E2E: AT-9 measures widths 1024/1194/1280/1366 for overflow and last-column clipping, AT-10 and AT-11 added, and CT-8 reads the visible twin through the shared `openEditor` helper (J5) instead of a hand-rolled row prefix.
- 2026-09-07 — C3-UX-1d: TIERED COLUMNS, NOT MIN-WIDTHS (C7 amendment). The attribute library's columns drop every `minWidth` and declare a tier plus a proportional width instead — Attribute `primary` w-[32%], Type `secondary` w-[14%], Options `wide` w-16 (≥xl only; the row expansion still carries the list), Used by `secondary` with no width (takes the remainder, chips wrap); `cardUntil` stays `lg`. Four floors summed past the scroller in the 1024–1279 band; tiers let the browser do the arithmetic. New guard `scripts/check-datatable-minwidth.sh` fails any DataTable column declaring `minWidth` without a same-line `// C7-dense:` justification, with `scripts/fixtures/bad-datatable-minwidth-example.tsx.txt` as its self-test fixture (`data-table.tsx` and the `dev.primitives` showcase are exempt by name, E5). INC-173b: the acquire-time language baseline now restores the FRESH-USER state — `profiles.preferred_language = NULL` via the service client (fixture write, J5; the product door cannot express "unset"), logging what it cleared; `switchLanguage` waits, after `html[lang]`, until the DEVICE record `localStorage["ethio.lang"]` reads the new code (≤10s) so navigation never races the write — selecting a language is a device choice; only STARRING syncs `profiles.preferred_language`, so a profile poll would have waited on a write that never happens (proved locally: RP-8/RP-11/RP-12 timed out on `null`). E2E: AT-9 now also asserts the ⋯ trigger visible on the first row and Options absent below 1280 / present at 1280+ at each of 1024/1194/1280/1366. CT-8 census: the four category-dialog verb Buttons already carry `size="touch"` (landed with L6) — no change needed.
- 2026-09-07 — C3-UX-2: ATTRIBUTES UNDER CATEGORIES + AMHARIC ATTRIBUTE LABELS + DENY PROOFS. The admin rail groups Categories · Attributes · Images under one default-open `ADMIN_GROUPS.categories` item derived from the section register (`NavItem.defaultOpen` is new on the shell primitive); each sub-item keeps its own permission gate and breadcrumbs read Group › Section (`breadcrumb-admin-group`). Additive migration (mark 20260907060000) extends `entity_source_value`, `admin_list_entity_translations`, `admin_entity_translation_stats`, `admin_save_entity_translation` and `admin_machine_entity_translation` to `entity_type='attribute'` / `field='label'`, so attributes translate through the existing Data scope; `useAttributeLabel` resolves every label render site through `entityName` with the EN definition name as fallback (option values stay EN — C3-UX-3). E2E: A-5 (group renders, expands, gates per sub-item), AT-12 (approved am label + EN fallback), AT-13 (Data roster pending), AT-14 (categories:view-only reads the library; `admin_upsert_attribute` / `admin_link_attribute` / `admin_delete_attribute` each refuse live and leave no trace).
- 2026-09-07 — C3-UX-2b: CLOSERS · LABEL FIELD · TWIN LOCATOR · GROUP TESTID. Corrective migration `20260907053238_2dcafad6` restates `REVOKE ALL … FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated` + `GRANT ALL TO service_role` for the five entity-translation definers redeclared by `20260907050122_84bead12` and READS BACK each ACL in-file (mark 20260907060100); that file is allowlisted in `scripts/migration-guard-allowlist.txt` citing the corrective (`2dcafad6`). PART D census: the console writes `field='label'` for `entity_type='attribute'` and `get_entity_bundle` shapes the map by field, but `entityName` read `["name"]` for every type — the TEST was right and the RESOLVER was wrong, so `src/i18n/entity.ts` now carries a `NAME_FIELD` map (category/location → `name`, attribute → `label`). AT-13's status badge is read through the twin surface and its own `entity-row-…(-card)` row instead of a bare page-level testid (J5). PART E: the group carrier declares `admin-group-categories` (new optional `NavItem.testid`, used only by group carriers) so it can never read as a section, and A-5 additionally asserts each grouped sub-item's page testid `admin-section-<id>`. Both i18n usage maps regenerated.
- 2026-09-07 — C3-UX-2c: EN BASELINE HEAL (INC-175) · SECTION TESTIDS · PORT GUARD. `global-setup`'s maintenance-owner section resets every non-scratch compiled EN key whose `ui_translations` `en` row drifted (value or status) back to the exact baseline `admin_sync_ui_keys` writes — value = compiled, status `approved`, machine false, origin `sync` — logging `healed N stale EN rows (INC-175)`; D3's "DB is runtime truth" scopes the PRODUCT database, staging is automation-only, and Amharic is untouched (the ratified-am tripwire stays the authority). `AdminCategoriesPage` and `AdminAttributesPage` roots now carry `data-testid="admin-section-<id>"` (the shell law; every grouped sub-item's page must answer it). ROOT CAUSE of A-1's stale copy AND TR-24/TR-26's 401s: `e2e:local` applied the staging env to the BUILD half only, so the served node build's `SUPABASE_URL` / `SUPABASE_PUBLISHABLE_KEY` / `E2E_FAKE_TRANSLATE` came from the sandbox — `/api/i18n/:lang` served the PROD bundle and `/api/translate` validated a staging JWT against prod. The serve/test half now carries the same env; `scripts/e2e-port-guard.ts` refuses to start when TCP 4173 is occupied (prints the listener PID, exit 1) and `playwright.config` sets `reuseExistingServer: false` whenever `E2E_LOCAL` is set, so a stale server can never answer with a stale env. The listings reaper also splits its two convictions (title, seller batches of 100) — one `or(seller_id.in.(…))` carrying every e2e seller finally exceeded PostgREST's query string and 400'd global-setup.
- 2026-09-07 — IE-1r: ATTRIBUTE EXPORT + PARITY REPAIR (INC-176) + REAL-DOOR PRIVATE MINTS + CATALOG. Migration `20260907173330_5a9c6351` re-declares `admin_export_attributes()` byte-identically to the deployed body (the IE-1 working tree was lost before its commit, so the connected project carried a function no migration file described), with in-file `REVOKE ALL … FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated` + `GRANT ALL TO service_role` and an ACL read-back (mark 20260907180000). New server route `src/routes/api/admin/attributes/export.ts` (`GET ?file=definitions|links`) authenticates from the bearer token through a caller-context publishable client, lets the RPC's own `categories:view` check refuse (403), logs `[ssr-error] /api/admin/attributes/export …` on every failure path, and answers CSV with a UTF-8 BOM, RFC-4180 escaping, a leading `'` on any `= + - @` cell and a dated `Content-Disposition` filename. The library toolbar carries ONE "Export attributes" control that pulls both files (no archive dependency, G2) and is disabled only while a download is in flight — never on an empty filter. `definitions.csv` = attribute_key, label_en, label_am, type, options, is_per_variant, direct_link_count (the normalized model has no per-variant flag today, so that column is emitted EMPTY rather than asserting `false`); `links.csv` = category_path, category_slug, attribute_key, is_required, is_filterable, card_rank, origin over the EFFECTIVE set (own ∪ ancestors, nearest link wins). DEC-041 amendment: `signIn`, `switchUser` and therefore `mintPrivateSuperAdmin` ignore `E2E_UI_LOGIN` and always drive the real form — `sessionInjectionEnabled()` now has no caller outside `session.ts`, and the knob stays in ci.yml / nightly-e2e.yml only until a harness DEC deletes it. The nav group label becomes `admin.nav.group.catalog` ("Catalog" / "ካታሎግ"); ids, routes and testids are unchanged. E2E: AT-15 (both downloads, verbatim headers, BOM, inherited origin = parent slug, `'=`-prefixed label), AT-16 (403 with a bearer that lacks `categories:view`, 401 with none, no control rendered).
- 2026-09-07 — C3-INH (DEC-044): the attribute console renders a filtered category's EFFECTIVE set, inherited rows read-only and named by their origin; the category editor's Attributes tab lists them the same way; the roster's two-must-display flag reads the effective card count; the export accepts a subtree scope (`?scope=<slug>`, slug in the filename). Migration mark 20260907190000 adds `admin_list_effective_category_links`, `admin_export_attributes(text)` and the effective card count on `admin_list_categories`. AT-17/AT-18/AT-19.
- 2026-09-07 — IE-2: ATTRIBUTE IMPORT — PREVIEW → CONFIRM/DISCARD → UNDO. Migration `20260907185010_5736cb8a` (mark 20260907200000) registers `categories:import` (step-up required, granted to `super_admin` only), creates the RPC-only capture table `attribute_import_revisions` (deny-all RLS, `GRANT ALL TO service_role`, batch index) and lands three gated doors over one shared planner: `attr_import_plan` (internal, no client EXECUTE — read back in-file), `admin_preview_attribute_import` (writes nothing), `admin_commit_attribute_import` (F5 order: permission → step-up → per-user advisory lock → capture old→new, batch-tagged → mutate; `change=none` writes nothing, so a round trip is idempotent) and `admin_undo_attribute_import` (restores the batch's untouched rows, audited). Refusals are PER ROW with a reason: missingKey, duplicateKey, badAction, unknownType, malformedOptions, badParent (DEC-045 — an option `parent` must exist on the `depends_on` definition's list), blastRadius, unknownCategory, unknownAttribute, outOfScope, inheritedRow (edit at the origin), badCardRank, formula. New server route `src/routes/api/admin/attributes/import.ts` (POST `mode=preview|commit|undo`) never trusts the client's parse: bearer → caller-context publishable client → server-side RFC-4180/BOM parse, 1 MB and 5 000-row caps, strict IE-1 headers plus an optional trailing `action` column (upsert · unlink · delete — deletion is never inferred from absence), a raw `= + - @` cell refused per row while the export's `'`-neutralised cell round-trips, and a SHA-256 digest over both files that the commit must match (409 `fileChanged`); every failure logs `[ssr-error] /api/admin/attributes/import …` and relays 401/403/409/413/428. UI: an "Import attributes" control beside Export, gated on `categories:import`, opening `ImportAttributesDialog` — file pick → preview counts and refusals → Confirm (through the page's step-up guard) or Discard, then "Undo last import" for that batch; an active category filter travels as the scope, so rows outside the subtree are refused in the preview. Keys EN+AM under `admin.attributes.import.*` plus `admin.roles.perm.action.import`. E2E: AT-20 round-trip no-op, AT-21 change → commit → undo (DB truth), AT-22 bad header / raw formula / unknown slug with nothing written, AT-23 no control and 403 for a `categories:view`-only scratch role (401 with no bearer), AT-24 edited bytes refused against the preview digest, AT-25 an invalid option `parent` refused.
- 2026-09-07 — IE-2b: IDEMPOTENT ROUND-TRIP (INC-177). Corrective migration (mark 20260907210000) re-declares `attr_import_plan` and adds two pure normalisers, `attr_option_norm(jsonb)` and `attr_json_or_null(text)` (in-file REVOKEs, DEC-022-B). ROOT CAUSE: every ratified definition stores its options as OBJECTS, IE-1 serialises them with `jsonb_array_elements_text` joined by `|` (so each segment is JSON text), and the planner split that back into plain STRINGS — so re-importing an untouched export reported every definition as "changed". The planner now parses a pipe segment beginning with `{` as JSON (a segment that starts like an object but does not parse is `malformedOptions`), and compares options SEMANTICALLY: `attr_option_norm` folds both sides to an ordered list of `{value, label_en, label_am, parent}` with null ≡ absent ≡ `""`, every field `btrim`-ed, the stored order preserved, a `{"depends_on": …}` meta entry excluded, and a bare string option read as value-only. `label_en` is compared through the same trim-and-default expression the write uses (blank falls back to the key), booleans are parsed from true/t/1/yes, card_rank empty ≡ null, and a DEC-045 `parent` is only validated when it is non-empty. IDEMPOTENCY PROOF IN THE MIGRATION: a DO block builds the whole current library in its own export shape (definitions with pipe-joined option text, own links with `origin = category_slug`), previews it through `attr_import_plan` and RAISEs unless adds = changes = refusals = 0. E2E: AT-20 becomes the invariant — the whole library is exported through the real route and re-imported through the DIALOG's file picker (`setInputFiles`), the preview must read 0 added · 0 changed · 0 unlinked · 0 deleted · 0 refused with `unchanged` = every data row of both files, and Discard leaves no `attribute_import_revisions` row (DB truth); AT-26 proves option key order and an explicit `"label_am": null` against an absent field preview unchanged.
- 2026-09-07 — IE-2b rider: AT-20 is now concurrency-proof. A sibling spec may mint or destroy its own scratch fixtures between the export and the preview, which read as a phantom add; the invariant therefore drops every exported record naming an `e2e_attr_` attribute or an `e2e-cat-` category (quote-aware record split, BOM + CRLF preserved) and asserts `unchanged` against the STABLE library rows alone. Local run: 52 passed (desktop-1280 + mobile-360).
- 2026-09-07 — CAT-IE: CATEGORY EXPORT + IMPORT (preview → confirm/discard → undo) THROUGH THE LIFECYCLE DOORS. Migration mark 20260907230000 creates the RPC-only capture table `category_import_revisions` (deny-all RLS, `GRANT ALL TO service_role`), the semantic normalisers and subtree helpers, and four gated doors: `admin_export_categories(text)` (`categories:view`, scope = a slug's subtree), `admin_preview_category_import` (writes nothing), `admin_commit_category_import` (F5 order: `categories:import` → step-up → per-user advisory lock → capture old→new batch-tagged → mutate, every mutation through the existing lifecycle RPCs so the cycle / catch-all / typed-slug / blast-radius laws keep one authority) and `admin_undo_category_import` (reverse order, audited). New routes `src/routes/api/admin/categories/{export,import}.ts`: BOM + RFC-4180 + formula-safe CSV over the 18 ratified columns with a dated, scope-named filename, and a server-side-only parse (1 MiB / 5 000 rows, strict header plus an optional trailing `action` — upsert · create-root · retire · reactivate · delete; deletion is never inferred from absence) with a SHA-256 digest the commit must match. The attributes import dialog was generalised into `src/features/admin/import-dialog.tsx` and both consoles now render presets of it (props, never a copy — B3): `ImportAttributesDialog` (two files) and `ImportCategoriesDialog` (one file). Roster toolbar: Export categories / Import categories, both honouring the root filter as the scope. Keys EN+AM under `admin.categories.export.*` / `admin.categories.import.*`. E2E: CT-18 round-trip invariant through the file picker (0 · 0 · 0 · 0 · 0 refused, Discard leaves no capture row), CT-19 create + rename → DB truth → undo, CT-20 retire/reactivate with both audit rows, CT-21 unknown parent / cycle / catch-all parent / delete-with-children / formula / duplicate slug with nothing written, CT-22 a `categories:view`-only operator (no control, 403, 401 anonymous), CT-23 a commit without step-up (428, P0009, nothing written). Local run: 99 passed, 3 skipped (7.8m) on desktop-1280 + mobile-360.
- IE-3 — column classes on both exports (derived columns marked read-only and never applied, reported as ignored cells), file-identity refusal before row parsing, guided refusals naming the fix, and a migration proof that the attributes plan and the categories plan cannot reach each other's doors (mark 20260908010000).
- 2026-09-07 — IE-3b: SILENT READ-ONLY REPORTER (INC-178), ONE SERIALIZER, GUIDED SLUG RENAME ON REAL DATA, GROUPED IGNORED PANEL. Corrective migration (mark 20260908040000; applied file 20260907221756) — corrective for 20260907185010 / 20260907191856 / 20260907194932 / 20260907202049, in-file REVOKEs restated for every DEFINER (DEC-022-B). PART A ONE SERIALIZER: the new server-only `attr_export_payload(text)` holds the attributes export body and emits EVERY cell as the exact text the CSV carries (booleans `true`/`false`, nulls `""`, card_rank `""` when unset, counts decimal, `is_per_variant` present and blank); `admin_export_attributes(text)` and its zero-argument wrapper are now gate + payload, `admin_export_categories` already served text, and both routes format NOTHING — they neutralise and quote. PART B READ-ONLY COMPARISON: `import_norm(column, value)` normalises booleans, pipes, dates, integers, nulls and whitespace, and `import_readonly_ignored` now reads its truth from the SAME serializer (no second derivation of `category_path`, `origin` or an approval-filtered `name_am`) and reports a cell ONLY when the normalised values differ — a column that merely exists is never reported. ROOT CAUSE of INC-178: the reporter re-derived read-only truth through its own helpers and compared raw text, so whole columns read as "edited" on an untouched download. PART C GUIDED SLUG RENAME: the detector moved to the TOP of `cat_import_plan`'s create branch (before the parent check, which a renamed ROOT never reached) and matches on the row's read-only address — `category_path` + `parent_slug` — refusing `slugRename` with the old slug as the fix; `import_guide_refusals` matches the same way. PROOFS IN THE MIGRATION: the live roster and the live attribute library, fed back through their own serializers, must show 0 ignored cells and 0 adds / 0 changes / 0 refusals, and the operator's real edit (Auto Parts `auto-parts` → `auto-part`) must decide as `slugRename` naming `auto-parts` with 0 adds. PART D PANEL: the Ignored panel groups by column (`{column} — {count} rows not applied`, EN+AM) with the row list one disclosure away; AT-20 and CT-18 now assert the panel is ABSENT on an unedited export, and CT-24 gains a rename attempt on a category holding children and a browse pointer.
- 2026-09-08 — IE-3c: MAP IN THE RECIPE, REFUSALS THAT SHOW THEIR VALUES, THE OPERATOR'S FILE REPRODUCED. PART A: `scripts/i18n-map-guard.ts` (`bun run i18n:map-guard`) hashes both usage maps, regenerates them and fails when a byte moved; `e2e:local` runs it FIRST, so a stale map fails locally before CI (A6 class rule). Both maps regenerated with this change (717 keys / 204 files). PART B: every refusal now travels with the cells it judged — `withRowValues` in the attributes import door joins each refusal to the operator's own parsed row by the row number they see (never re-derived) and both import routes attach it to preview AND commit payloads; `ImportDialog` interpolates every column name plus the server's `detail`, and the EN+AM reason texts were rewritten to name their values ("Unknown parent category: parent_slug 'x' is not in the roster or in this file (row slug 'y', path 'z')"), the slug-rename guidance naming old → new. No migration: the planners' verdicts and reasons are unchanged; the values come from the operator's file, joined in the door. PART C: the uploaded `categories.csv` (BOM, CRLF, Excel quoting, 154 rows) previewed against the connected project through `admin_preview_category_import` with a throw-away super-admin identity: 1 add (`theater` under `electronics`), 8 changes (display_order on the vehicles branch, `expiry_days` 45 on `coupes`), 145 unchanged, 0 refusals, 0 ignored cells — the decision path produced no refused row, so no detector change was warranted. CT-24 gains part (d): the operator's row shape (quoted comma name, Amharic `name_am`, filled read-only address columns) with a parent typo, asserting the refusal is `unknownParent` and carries `parent_slug`, `category_slug` and `category_path` as judged values, with 0 adds.
- 2026-09-08 — IE-4a: AMHARIC CATEGORY NAMES THROUGH THE TRANSLATION DOOR. Corrective migration (mark 20260908060000; applied file 20260908022337) re-declares `import_readonly_ignored`, `cat_import_plan`, `admin_commit_category_import` and `admin_undo_category_import` whole, with their REVOKE/GRANT restated in file (DEC-022-B). `name_am` LEAVES the categories read-only list and becomes an editable column: a non-empty cell is written through `admin_save_entity_translation`, so it lands as a HUMAN row awaiting review (`status='edited'`, `machine=false`) — the import never auto-approves; an identical value writes nothing; an EMPTY cell is SILENCE (the planner only ever emits a non-empty, differing am value), so a translation is never deleted by a blank spreadsheet cell. COMMIT captures the prior am state (`am_state`: none, or value + status + machine) beside the exported row and UNDO restores that tuple exactly — an approved row comes back approved rather than demoted by a human write — or removes the pending row the batch itself created; deletes drop the category's am rows with the row and restore them from the capture. Category delete refusals now NAME the children that hold the row down (`detail` = comma list, `children` = array) instead of only counting them. PROOFS IN THE MIGRATION: the live roster in its own export shape plans 0 adds / 0 changes / 0 refusals, the same roster with EVERY am cell blanked plans 0 changes (silence), and the planner/reporter stay closed to anon + authenticated while the two doors stay authenticated-only. E2E: CT-26 (root + child + grandchild created in one file with Amharic names → pending rows asserted through the service client, the blank cell wrote nothing, a re-preview is a no-op, undo removes categories AND the rows the batch created); CT-27 (a retired leaf carrying an APPROVED am name deletes and undoes with that exact state, and deleting its parent is refused `hasChildren` naming the child slug, with nothing written by the preview).
- 2026-09-08 — IE-4a CORRECTIVE (mark 20260908070000): the category import planner now resolves a parent created earlier in the SAME file, so one import can create root+child+grandchild (the commit already landed creates parents-before-children); a parent found neither in the library nor earlier in the file is still refused unknownParent. Live migration proofs cover both. CT-26/CT-27 green on desktop+mobile.
- 2026-09-08 — IE-4b: AMHARIC ATTRIBUTE LABELS THROUGH THE TRANSLATION DOOR. Corrective migration (mark 20260908080000; applied file 20260908032617) re-declares `import_readonly_ignored`, `attr_import_plan`, `admin_commit_attribute_import` and `admin_undo_attribute_import` whole, with their REVOKE/GRANT restated in file (DEC-022-B). `label_am` LEAVES the attributes read-only list and becomes an editable column under the same law IE-4a gave `name_am`: a non-empty cell is written through `admin_save_entity_translation('attribute', id, 'label', 'am', …)` so it lands as a HUMAN row awaiting review (`status='edited'`, `machine=false`) — the import never auto-approves; an identical value writes nothing; an EMPTY cell is SILENCE, so a blank spreadsheet cell never deletes a translation. COMMIT captures the prior am state (`am_state`: none, or value + status + machine) beside the exported row and UNDO restores that tuple exactly — an approved row comes back approved rather than demoted by a human write — or removes the pending row the batch itself created; a delete drops the attribute's am rows with it and restores them from the capture. Attribute delete refusals now NAME the linked categories holding the definition down (`detail` = comma list, `categories` = array) instead of only counting them, and the EN+AM `blastRadius` texts render that list. PROOFS IN THE MIGRATION: the live library in its own export shape plans 0 adds / 0 changes / 0 refusals, and the same library with EVERY label_am cell blanked plans 0 changes (silence). E2E: AT-29 (definition + link created in one file with an Amharic label → the pending row asserted through the service client, no ignored-column report, a blank cell writes nothing, undo removes the definition AND the row the batch created); AT-30 (an unlinked delete applies and undo restores its APPROVED am row with that exact status, while a linked delete is refused `blastRadius` naming the category slug, with the refused definition untouched).
- 2026-09-08 — IE-4b CORRECTIVE (mark 20260908090000): the attribute import Undo loop ordered its captured rows by `id DESC`, but `id` is a random uuid — the order was arbitrary, so a definition the batch created could be visited while its own link still stood, hit the "now carries links" guard and stay behind. The loop now walks LINKS FIRST (they depend on definitions), then definitions, newest capture first inside each kind. No behaviour other than order changes.
- 2026-09-08 — DEC-045a: DEPENDENT OPTIONS (schema, server law, console cascade, read-only export column). Migration mark 20260908110000 adds nullable `attributes.depends_on` (self FK + index), the helpers `attr_option_values` and `attr_dep_cycle`, and re-declares `admin_upsert_attribute` with `p_depends_on`, validating unknown parent (`dependsUnknown`), non-single_select parent (`dependsNotSelect`), self-reference (`dependsSelf`), cycles (`dependsCycle`), a `parent` value outside the parent's list (`parentNotInParent`) and a `parent` with no dependency declared (`parentWithoutDepends`). `admin_delete_attribute`, `admin_unlink_attribute` and `admin_merge_attributes` refuse while dependents exist and NAME them. The library, link and effective-link readers report `depends_on_key`; `attr_export_payload` emits a `depends_on` definitions column, READ-ONLY in 045a (the importer never applies it — 045b makes it importable). Console: a "Depends on" picker gated on the write permission, one options editor per parent value, and a cascade preview in both the editor and the per-category link manager. Keys EN+AM. Proofs AT-31/AT-33/AT-34.
- 2026-09-08 — DEC-045a-fix: CLOSERS + J6 ISOLATION. Corrective migration (mark 20260908120000; applied file 20260908050101) restates `REVOKE ALL … FROM PUBLIC, anon` · `GRANT EXECUTE … TO authenticated` · `GRANT ALL … TO service_role` for `admin_delete_attribute(uuid, text)`, `admin_unlink_attribute(uuid)` and `admin_merge_attributes(uuid, uuid[])`, which DEC-045a re-declared without restating their ACLs, and reads the result back in-file with a loop that raises unless each door is executable by authenticated + service*role and by nobody else (DEC-022-B). `scripts/migration-guard-allowlist.txt` carries file 20260908041703 citing this closer (d9267b5f). J6: CT-18 and AT-20 no longer read the capture tables globally — "Discard wrote nothing" is now "no `attribute_import_revisions` / `category_import_revisions` row whose `created_by` is THIS test's pooled actor since `startedAt`", so a parallel worker's legitimate commit can no longer indict them. AT-13 likewise scopes its "still pending" claim to `entity_translations.updated_by = this test's actor`. CENSUS: the bulk machine-translate sweep is scoped by LANGUAGE only (the zxx-*/zxy-\_ fences) and honours no key prefix, so the attribute-key fence has no filter to ride and the isolation is by actor alone.
- 2026-09-08 — DEC-045b: IMPORTABLE `depends_on` + OPTION LABELS + CASCADE LABELS + THE RENDER INVARIANT. Migration mark 20260908130000 (applied file 20260908072145) re-declares `attr_import_plan`, `admin_commit_attribute_import` and `admin_undo_attribute_import` whole, each restating `REVOKE ALL … FROM PUBLIC, anon` · `GRANT EXECUTE … TO authenticated` · `GRANT ALL … TO service_role` with an in-file ACL read-back loop (DEC-022-B). The definitions file's `depends_on` column becomes EDITABLE: the planner resolves a parent declared in the same file, orders creates parent-before-dependent, and refuses `unknownParent`, `dependsNotSelect`, `dependsSelf`, `dependsCycle` and `badParent`; commit resolves the key to the parent id and undo breaks batch-created edges before deleting and restores prior edges. Both attribute routes carry `depends_on` bare in their column list. UI: the library's Options expansion renders option LABELS (value as fallback) grouped by parent value for dependent definitions — the source of the "[object Object]" leak — and the link manager's cascade preview names the parent and the child (keys EN+AM). HARNESS: `gotoReady` and `findRow` fail on `[object Object]`, a standalone `undefined` or `NaN` in the rendered body, with a page dump. Proofs AT-32, AT-35.
- 2026-09-08 — D-GATE-3: GOVERNANCE IMPORT (docs only, verbatim). Imported the catalog-curation thread handoff byte-identical to `docs/governance/handoffs/2026-09-08-catalog-curation-thread-handoff.md` (sha256 9f60448270521549d83d4750219243d10a7c5e35ed876a2a52ff7228228c553a); appended section A of the S34/S35 backfill to `docs/spec/spec-ledger.md` as "S35 — Stabilisation, import/export and dependent options" (DEC-036 amended, DEC-038…DEC-045, DEC-023 LIVE, the INC-160…INC-178 summary line, supervisor slips); replaced the "Current position" section of `docs/governance/system-state.md` with section B (S35 closed; NEXT = the catalog curation thread); registered INC-160…INC-178 in `docs/tracking/incidental-findings.md`, numbering continued and nothing renumbered; recorded section C as `docs/governance/knowledge-amendments.md` (Knowledge v3.7: A8, C7 amended, F5 amended, J10) and section D as `docs/governance/instructions-amendments.md` (supervisor v1.9: G23…G26). No src, no e2e, no migrations.
- 2026-09-08 — INH-1: INHERITANCE FOLLOWS PRIMARY LINEAGE ONLY (DEC-044 amendment). Migration mark `20260908140000` adds `public.cat_primary_parent(uuid)` — the one reader of the primary edge `(parent_id IS NOT NULL, display_order, created_at)` with an active parent — and routes every effective-set walk through it: `admin_list_effective_category_links` (console inherited rows), `attr_export_payload` (ancestor walk AND the `?scope=` subtree), `attr_import_plan` (the scope subtree behind `outOfScope`, rewritten surgically in-file with a guarded assertion so its 480-line body is otherwise byte-identical) and `admin_list_categories` (the EFFECTIVE `card_attribute_count` behind the amber two-must-display flag). A secondary (browse) pointer now confers nothing. Closers restated in-file for all five functions with an ACL read-back loop, plus a behavioural proof (secondary confers 0 links, primary confers 1) built and removed inside the file. Spec: AT-36.
- 2026-09-09 — IE-5: PLAN DEFINITION DELETES AFTER LINK REMOVALS (one file, one pass). Migration mark `20260909040000` (applied file `20260909034642`) rewrites `attr_import_plan` surgically in-file under a guarded assertion — a pre-pass collects the links file's in-scope `unlink` pairs (`<category slug>|<attribute key>`) before any definition verdict, and the `blastRadius` refusal now reads only the links that SURVIVE those unlinks, still naming the categories it judged. `admin_commit_attribute_import` is re-declared in four phases (definition upserts → link upserts → link unlinks → definition deletes) with per-phase capture-then-mutate and batch-tagged revisions; `admin_undo_attribute_import` is re-declared with the REVERSED ordering (definition deletes restored first, then links, then this batch's own creations removed last) so an unlink+delete batch undoes without conflicts. Undo was outside the stated scope but had to move with it: the old links-first ordering restored a link whose attribute did not exist yet and counted it `conflicted`. Closers restated in-file for all three definers with an ACL read-back loop. Spec: AT-37.
- 2026-09-09 — IE-5 (corrective, two files): THE PLANNER PATCH WAS TEXT-DEPENDENT. `20260909034642` rewrites `attr_import_plan` by matching its body text and raised `IE-5: the planner declaration block did not match` on staging, whose body text differs; it can never write its declared mark `20260909040000` because it raises first, so that file is SKIPPED on any copy that refuses it. Its whole payload is re-landed text-independently: part one (mark `20260909041000`) declares `attr_import_plan` IN FULL — the post-INH-1 planner plus the IE-5 survivor rule (a pre-pass collects the links file's in-scope `unlink` pairs and the `blastRadius` verdict reads only the links that survive them) — with an in-file proof asserting both the survivor rule and the INH-1 primary-lineage walk; part two (mark `20260909042000`) declares the four-phase `admin_commit_attribute_import` and the reverse-order `admin_undo_attribute_import`, and heals the ledger by recording `20260909040000` so migration parity holds with the skipped file. Closers restated in-file with ACL read-backs in both. Behaviour is identical to the IE-5 landing; spec AT-37 unchanged.
- 2026-09-09 — IE-6: DIRECT ROWS OVER INHERITED ECHOES (INC-180) + EMPTY READ-ONLY CELLS ARE SILENCE (INC-181). Migration mark `20260909050000` (applied file `20260909045217`) declares `attr_import_plan` and `import_readonly_ignored` in full, text-independently. The links loop now reads DIRECTNESS first (`origin` empty or equal to the row's own category slug): an inherited echo row never registers in the duplicate set and never refuses, so a direct row for the same category and key in the same file is accepted and becomes the nearest link; unlinking through an echo is still refused (`inheritedRow`). The read-only reporter skips EMPTY cells — "not provided" is never an edit — and reports only a non-empty read-only cell that differs from live truth. Closers restated in-file for both definers with an ACL read-back, plus a behavioural proof (echo + direct row previews 1 add · 0 refusals; empty read-only cells report nothing) built and removed inside the file. Spec: AT-38, AT-39.
- 2026-09-09 — IMPORT-GATE (T1): ONE SECURITY GATE FOR EVERY IMPORT. Part A lands `src/server/imports/registry.ts` (one declaration per family: permission, step-up, scope kind, per-file identity header, ordered columns with a class each — identity/editable/read-only/action — identity columns, duplicate policy and caps) and `src/server/imports/gate.ts` (bearer to caller-context client, family lookup, scope shape, BOM-aware RFC-4180 parse, file identity before any row, exact header law with no duplicate or unknown column, NFC plus control/bidi/zero-width stripping, NUL and raw-formula refusal, per-column format and length law, duplicate identities, typed row payload, SHA-256 digest, one in-flight door and 30 previews a minute per operator, and an audit call per door). Part B is migration mark `20260909060000` (applied file `20260909054432`): `import_norm_text`, `import_norm_bool`, `import_norm_date`, `import_norm_pipe`, `import_is_slug`, `import_sanitize` and `import_gate_audit`, each with in-file `REVOKE ALL FROM PUBLIC, anon` / `GRANT EXECUTE TO authenticated` / `GRANT ALL TO service_role` and an ACL read-back. The attributes and categories import routes and both export routes now enter through the gate and own no judgement of their own; refusal vocabulary, digest and RPC contracts are unchanged (`badHeader` stays whole-file nonsense, `wrongFile` stays another family's header, `duplicateSlug` stays the category planner's verdict). Part D is `e2e/import-security.spec.ts`, parameterised over every registered family (IG-1 whole-file refusals, IG-2 per-row hostility, IG-3 digest and rate limit). Part E is `scripts/check-import-gate.sh` with its bad-route fixture, wired into CI, and `docs/features/imports.md`. Two laws the run taught the gate: identity before shape (`requireBearer` at the door, so an anonymous caller is 401 and never learns whether the body was well formed — CT-22), and option cells are the platform's own serialisation (whole-cell JSON array, or pipe segments that are each a JSON object or `value=label`), so the gate measures only their SIZE and leaves meaning — bad parent, malformed array — to the planner (AT-20, AT-25). Translations CSV/XLIFF join the gate in T2.
- 2026-09-09 — IMPORT-GATE (T2, Part C + Part D): THE UI-STRING DOOR JOINS THE GATE. `POST /api/admin/translations/import` is the only way a translation file now reaches the database: the console reads the file as text and posts it with the target language, the browser parses nothing, and `translations` is a registered family (`translations:manage`, language scope, key/value law `[A-Za-z0-9][A-Za-z0-9._:-]*`/200 and 4000 characters, `context` an optional trailing column). Both dialects enter the same door — XLIFF 1.2 units become the family's own columns before any refusal is spoken, and a unit with no id or no target yields an EMPTY row rather than being dropped so an operator's row numbers stay the file's own. The export now NEUTRALISES a formula-leading cell with a leading apostrophe and the gate un-neutralises on the way in, so `+ Add` survives a round trip while a raw formula in a hand-made file is refused. The family has no preview door and no digest, so the WRITE carries the one-in-flight and per-minute budget; `undo` takes the same route WITH its batch's language, so a take-back is scoped, metered and audited exactly like the run it undoes. `admin_import_translations` and `admin_undo_import` keep EXECUTE for `authenticated` and remain the sole authority on permission, step-up, unknown keys, placeholders and revision capture. `scripts/check-import-gate.sh` gained a fourth rule: an import writer called outside its route is a second door and fails the build. Part D's hostile catalogue is now parameterised over three families and gained mode-awareness (a one-step family meters a row the gate itself refuses, so the ceiling is proven with no write) plus IG-4, the XLIFF dialect. Runs: import-security 20 passed; translations console + data 41 passed; governance 14 passed with TR-19/TR-30 (`@global-state`, DEC-028) failing on catalog-page ordering unrelated to the door — INC-182.
- 2026-09-09 — IMPORT-GATE (T3): CLOSURE RUN + INC-182 CLOSED. The mandatory run across all three families on both projects (`admin-attributes`, `admin-categories-console`, `admin-categories-lifecycle`, `admin-translations-console`, `admin-translations-data`, `admin-translations-governance`, `import-security`) is green: 211 passed, 7 skipped, 0 failed (22.4m, desktop-1280 + mobile-360). INC-182 is closed at the anchor (J7): TR-19 opens the fence page already narrowed to its own scratch prefix through the list's URL search (`?q=`) instead of reading page one of a 1063-key catalog, and TR-30 — whose real fault the subset run had masked — reads the publish switch and its gate caption through `langRow(...)` per DataTable twin, a bare `lang-public-zxa` having resolved to both twins (J5). Test-only change; no src, no migration.
- 2026-09-09 — IE-7: IMPORT DIALOG STATES (UI only). The shared shell now renders ONE state at a time. Ready gives every declared file a real picker — a touch-sized secondary "Choose … file…" button over a visually hidden native input (the testids the specs address are unchanged), with the chosen filename beside it — and Preview stays disabled until EVERY required file is chosen. Previewed keeps the counts, refusals and Ignored panel with exactly Discard (tooltip: close without writing anything) and Confirm. Applied adds a success banner, "Import applied — N changes written", counting the writes alone; the counts stay, and the footer holds exactly Undo last import + Close, with Confirm and Discard not rendered. EN+AM keys ship together (`choose`, `noFile`, `discardHint`, `applied`, `close`; the old `committed` line retires). Spec: AT-40, CT-28 drive a scratch file to Applied and assert DB truth after Undo. No src outside the shell and the locales; no migration.
- 2026-09-09 — FIX-SCAN-1: SCANNER FINDINGS AT THEIR ROOTS. (1) `admin_update_attribute_link` (migration mark `20260909150000`, applied file `20260909142634`, healing the unmarked `20260909140408`) makes Required/Filterable ONE atomic UPDATE under the same `categories:update` + step-up gate the link/unlink pair carried, F5 order with capture-before-mutate and an audit row; the link manager no longer unlinks-and-relinks, so `card_rank` survives a toggle and a failed call cannot lose the link (AT-42, DB truth both ways). (2) DEC-047 amends the DEC-040 completion contract: `guard()` now SETTLES on every path — `StepUpCancelled` on cancel/close, `StepUpUnavailable` when no factor exists — so every caller resets its pending state, claims nothing happened (F4) and renders the translated `mfa.stepUpUnavailableHint` for the no-factor case; every `guard(` consumer (users, roles, translations, categories, attributes, imports, impersonation, images) routes through the shared `stepUpAbortKey()` (MF-7b, MF-7c). (3) The translations CSV/XLIFF export controls enable on the CATALOG, never on the filtered page — the export always read the whole language (TR-33). (4) ONE icon allowlist (`src/lib/category-icon-names.ts`, 115 names — the old 63-name server list unioned with every name the ratified taxonomy actually stores) and ONE total glyph map (`src/components/shell/category-glyphs.ts`) now serve the suggester, the rail, the roster and the editor preview; the roster's `data-icon` names what actually rendered so drift fails loudly (CT-29). (5) Issue 5 is VERIFIED WORKING, not fixed: `raster.ts` already sniffs magic bytes and decodes PNG through `pngjs`; `src/server/category-images/raster.test.ts` is the standing proof — the pipeline's own PNG, a foreign-encoder PNG with no alpha channel (`scripts/fixtures/category-image-provider.png`), and the whole generation pipeline over PNG bytes.
- 2026-09-09 — D-GATE-4: GOVERNANCE IMPORT (docs only). Spec ledger S35 appends DEC-046 (the import gate: one server entry, registry-declared column classes, cell hygiene, rate limit, CI guard, parameterised hostile-file spec), DEC-047 (guard cancel contract: StepUpCancelled/StepUpUnavailable; DEC-040 amendment), DEC-048 candidate (delete the inert E2E_UI_LOGIN knob), INC-179…182 summaries, the scanner-findings 1–5 resolution record, and the Vehicles curation record. System-state current position updated (catalog surfaces complete incl. the import gate; Vehicles curated; Real Estate next; open: DEC-048, INC-174, C3-UX-3/4, the C3 era gate review). INC-179/180/181 entries added to incidental-findings. No src, no e2e, no migration.
- 2026-09-10 — DEC-048: the inert `E2E_UI_LOGIN` knob is deleted. Removed from all four E2E jobs in `ci.yml` (`nightly-e2e.yml` never set it) and from the mirrored `e2e:local` runtime env in `package.json`; `sessionInjectionEnabled()` and the now-dead `uiLoginForced()` / `isAuthSpec()` it alone consumed are gone from `e2e/helpers/session.ts` (grep proof: zero callers outside the file). DEC-041 had already made the routing unconditional — pool injects, private mints and auth specs take the real door — so the knob could revert nothing. Frozen rule: the next push run and the next nightly must be green with it gone; any behaviour change in a `@private-identity` test reverts this DEC in full. No src, no migrations, nothing else in the workflows.
- 2026-09-10 — UX-2: CATALOG POLISH FROM THE ERA WALK. Icons: one allowlist resolves every ratified category (not roots only), catch-alls take a fixed "more" glyph; CT-29 extends to every ratified row. Toolbars: Categories reads [Search · Root filter] [Missing assets · Generate missing (max 25), tooltip naming the DEC-032 spend cap] [Export · Import], Attributes [Search · Category filter] [Export · Import]. Status: a future `visible_from` reads Scheduled in both twins (`isScheduled()`, CT-4) and the date-window FLAG no longer renders on those rows — the two keys can carry the same word in the runtime catalog, so the duplicate is suppressed in the roster, not in the wording. Attribute dialog: helper text under Key (machine identity, snake_case, never changes) and Name (what people see; translatable), EN+AM. Link manager: each Required/Filterable atomic write reports "Saved" beside its toggle until the next change (a timed disappearance is feedback a reader — and AT-42 — can miss), failures stay the inline error line (F4), and the footer reads Done with "Changes save as you go". IE-8 rename detector: a new key whose `label_en` + type + options match a definition absent from the file is refused with restore-the-old-key guidance (AT-43). Migrations `20260910042749_afcb289a` / `20260910045047_1dce2042` are superseded by `20260910053535_40e4ab7d`, which re-declares `attr_import_plan` WHOLE with its closers and an in-file read-back — INC-183 (second occurrence): a migration never patches a function body by text anchor, flexible or not.
- 2026-09-10 — UX-2 item 7: the Data-scope roster renders each row's stable identity under the name (category slug · attribute key; a location has none and renders nothing), so duplicate labels are distinguishable. Migration `20260910064921_d0a7a0af` re-declares `admin_list_entity_translations` WHOLE (INC-183; return shape changed, so drop + full declaration) with one added `identifier` column — gate, universe, filters, ordering, paging and `total_count` unchanged — with REVOKE/GRANT closers and definition + ACL read-back in-file (apply `d0a7a0af` → expect mark `20260910080000`). E2E: shared `entityRow` twin helper adopted by TR-14/TR-24/TR-26 (anchors unchanged) and TR-34 added — duplicate "Make" labels told apart by key, a category by its slug, plus the round-trip invariant that `get_entity_bundle` and both attribute export files are byte-identical across the roster read (transient e2e fixtures excluded, J6). Run: `e2e/admin-translations-data.spec.ts` 8 passed on both projects.
- 2026-09-10 — D-GATE-6: HANDOFF IMPORT (docs only, verbatim). Imported `2026-09-10-engineering-thread-handoff.md` byte-identical to `docs/governance/handoffs/` (sha256 `4f0d607bf1caea7b917ac57fc83161bce9444ea1601bd9be55713f7c41112386`); updated system-state NEXT to DEC-049 → INC-174 → U6 spec session; registered DEC-049 (candidate) and INC-183 in the spec ledger; one changelog line. No src, no e2e, no migrations.
- 2026-09-10 — DEC-049: WORKFLOW ACTIONS ON THEIR NODE-24-NATIVE MAJORS. Every `uses:` of the four actions the DEC names moves to the first major whose `action.yml` declares `runs.using: node24`: `actions/cache@v4` → `@v5` (13 sites: ci.yml ×10, nightly-e2e.yml ×2, guard-proof.yml ×1), `actions/upload-artifact@v4` → `@v6` (17: ci.yml ×15, nightly-e2e.yml ×2), `actions/download-artifact@v4` → `@v7` (7, ci.yml), `gitleaks/gitleaks-action@v2` → `@v3` (1, ci.yml); the pin in `docs/features/ci-guards.md` follows. Nothing else in the workflows moves — no `with:` key, job name, `if:`, comment or other action; `actions/checkout@v4` ×3 and `actions/github-script@v7` in ci-status-report.yml / guard-proof.yml are the DEC-049b residue. GitHub's runners have defaulted to Node 24 since 2026-06-16 and remove Node 20 on 2026-09-23 (changelog, updated 2026-08-25); these workflows set no opt-out, so the declared runtime now matches the one already in use. Judge, pre-committed: this push's run and the next nightly green, else revert in full; guard-proof.yml's one line is dispatch-only and unexercised by the judge. No src, no e2e, no migrations.
- 2026-09-10 — D-GATE-7: RECORDS LANDING (docs only, verbatim). Lovable Project Knowledge v3.8 installed in the settings box and mirrored WHOLE to `docs/governance/lovable-knowledge.md` (the mirror had stood at v3.1 since 2026-07-29 despite its same-session rule; knowledge text sha256 `e0501dcfbf3acd2743f0b06cf64ebba7dd5b6dc84016083f4ae724582086fefc`); `knowledge-amendments.md` records the v3.8 delta (A8: INC-183 whole re-declaration; two trims for the settings box's 10,000-character cap; INC-179 deferred to v3.9). Spec ledger S36: DEC-048 RATIFIED (judge met), DEC-049 LANDED at `c6e5fe5` (push green and promoted; nightly 2026-09-11 pending), DEC-049b candidate (checkout@v4 ×3, github-script@v7), INC-184 (TR-29 J6 invariant leak), INC-185 (TR-12 unlabeled waits), INC-186 (green runs never judge the artifact contract), ACT-C3-1 (guard-proof currency), ACT-U4-6/ACT-U4-7 closed, three supervisor slips with a G16 addendum proposal. system-state: Standing reads → instructions v1.9 · Knowledge v3.8; NEXT: INC-174 → Real Estate import → U6 spec session. No src, no e2e, no migrations.
- 2026-09-11 — D-GATE-8: RECORDS (docs only, verbatim). DEC-049 RATIFIED (nightly 34570333823 green on `b0a4bd2`; judge met in full); DEC-048 record gains the #407 cancelled-lane clause; INC-174 CLOSED with its cause (pre-c40b7109 local session injection re-armed by the INC-120 sentinel after RP-1's storage clear; verified 2×2 green at HEAD, no code change); ACT-C3-2 (e2e:local browser preflight); TR-34 watched; queue corrected to the roadmap's DEC-033 order — Locations era + posting-prerequisites census (C4 tags disposition open) before U6; system-state NEXT updated. No src, no e2e, no migrations.
- 2026-09-11 — D-GATE-9: RECORDS (docs only, verbatim). Real Estate curation pass recorded (fourteen decisions, pre-preview file validation, console counts, 131 changes written, zoning→land_use merge, Amharic approved); INC-187 registered — the categories import plans `display_order` but never commits it (the planner's delta carries it; the commit passes NULL to `admin_update_category`; `admin_create_category` has no order parameter) — fix next, ahead of the Electronics import; D-GATE-8 DRIFT corrected (the INC-174 paragraph had been double-spaced by the paste; content was verbatim); system-state NEXT updated. No src, no e2e, no migrations.

- 2026-09-11 — INC-187 FIX, PART 1: THE CATEGORIES IMPORT APPLIES ORDER ONCE PER PARENT. Migration `20260911100932_5c1e174e` re-declares `cat_import_plan`, `admin_commit_category_import` and `admin_undo_category_import` WHOLE (INC-183) with closers and definition + ACL read-back in-file: the planner emits no `display_order` delta for a catch-all (the reorder door pins them); the commit's per-row rank-and-renumber block is gone — after every create and update, one ordering pass per primary parent sorts siblings by the file's `display_order` for rows in the batch and their current position otherwise and calls `admin_reorder_categories` once, so a file's sequence lands as written and created rows take their stated place; undo restores each batch row's previous order the same way. Mechanism corrected in INC-187 (the earlier "never commits it" was a truncated-grep ruling). docs/features/categories.md gains the order paragraph. (apply `5c1e174e` → expect mark `20260911120000`.) Part 2 next: CT-30 proof and the Real Estate categories re-import. No src, no e2e.

- 2026-09-11 — INC-187 FIX, PART 2: CT-30 proves the order pass — a reversed sibling order plus a created row placed mid-sequence lands as the file's sequence (0,1,2,3,4 by the file's order), a catch-all's edited order cell counts as no change and the row stays pinned at ≥1000000, and undo restores the original order and removes the created row; DB truth read through the service client (J4). Built on CT-19's machinery; no helper changes. Green locally on both projects (DEC-023). INC-187 closes on the Real Estate categories re-import walk. No src, no migrations.
- 2026-09-11 — C3-UX-5: THE CATEGORIES FILTER IS A GROUPED SUBTREE PICKER. The roster's category filter lists every category in depth-first roster order with children indented under their parent (the attributes console's shape), marks retired rows with a translated suffix and the muted token, and scopes the roster and Export subtree to the selected category's subtree; one shared option builder serves both consoles; options carry `data-depth` / `data-parent-id` / `data-retired` for structural anchoring. Keys `admin.categories.filter.allCategories` and `admin.categories.filter.retired` (EN+AM). CT-31 proves grouping, marker and scoping by structure; green locally on both projects (DEC-023). Records: INC-187 CLOSED on the Real Estate re-import walk; part-2 corrections logged; ACT-C3-2 raised. No migrations.
- 2026-09-11 — ACT-C3-2: e2e:local VERIFIES THE BROWSER FIRST. New `scripts/e2e-browser-guard.ts` runs after the port guard and before the build: resolves the browser exactly as playwright.config.ts does (`E2E_CHROMIUM_PATH`, else the Playwright-managed Chromium), exits non-zero naming the resolved path and the remedies (`bunx playwright install chromium` or `E2E_CHROMIUM_PATH=<binary>`) when it is missing. Proven: fails on `E2E_CHROMIUM_PATH=/nonexistent`, passes in the executor image. Docs: e2e-harness.md. CI untouched; no e2e specs, no migrations.
- 2026-09-11 — D-GATE-10: ATTRIBUTE SYSTEM REVIEW RATIFIED (docs only, verbatim). `docs/governance/reviews/attribute-system-deep-dive-2026-09-11.md` imported byte-identical (sha256 `b8c928d03bb1ee94cf35a688344486ecb610b9c573bea11df2675f2cfede1d6a`); ledger S36 addendum 4 records DEC-050 (definition v2: units, bounds, relative year tokens, option-conditioned bounds, option active/aliases, text presets, Amharic help text, `range` retired, option coverage), DEC-051 (single listing-attribute validator and the `attrs` shape at U6-A), DEC-052 (category `capabilities`; Bookings era after U8, request-only), DEC-053 (lazy, version-cached option lists; the cap raised on evidence only), standing curation rules R1/R2, and the operator's sequencing (P1 before the eleven remaining passes and before Locations — a vertical curated before P1 needs a backfill, one curated after fills the cells once); roadmap gains Next 1b, the U6-A note and the Bookings item; curation handoff gains §7; system-state NEXT rewritten. No src, no e2e, no migrations.
- 2026-09-11 — DEC-050 L1: ATTRIBUTE DEFINITION v2 — THE MIGRATION. `20260911193208_d42d6246` (mark `20260911200000`) adds `unit`, `min_bound`, `max_bound`, `decimals`, `format`, `preset`, `max_length`, `help_text_am` to `public.attributes` with CHECKs, adds the optional option keys `active`/`bounds`/`aliases` behind a strict shape that refuses unknown keys by name, retires the dead `range` type (zero rows), and re-declares `admin_upsert_attribute` (whole, INC-183; 15 args / 9 defaults), `attr_import_plan`, `admin_commit_attribute_import` and `admin_undo_attribute_import` so every present cell is judged, diffed, applied and undone; bounds targets must be `number` definitions co-linked in every category where the owner is linked (LIVE at the door, POST-PLAN at the planner); new gated read `admin_attribute_option_coverage()`. Spec: `docs/governance/dec-050-spec.md` (sha256 `8428493f…`). Eight in-file proofs green; closers restated and read back. No src, no e2e in this turn.
- 2026-09-11 — DEC-050 L2a: EFFECTIVE CARD-RANK UNIQUENESS. Migration `20260911223202_ee224de0`: `attr_import_plan` and `admin_set_card_attributes` re-declared whole to refuse a direct card rank equal to an inherited rank in the same category (inherited by primary lineage), naming both origins — the DB's `UNIQUE (category_id, card_rank)` guards direct links only; proofs in-file. attributes.md gains the L2a paragraph. (apply `ee224de0` → expect mark `20260911223000`.) No src, no e2e.
- 2026-09-11 — LINTER CLOSERS. Migration `20260911223744_9d74f733` (mark `20260911230000`, ACLs only, no bodies): `has_permission` and `get_my_permissions` revoked from PUBLIC and anon (granted to authenticated + service_role; no anon-role policy calls them), and the four guard bodies `rls_auto_enable`, `role_permissions_core_lock`, `roles_system_lock`, `user_roles_protect` revoked from PUBLIC, anon and authenticated (trigger firing ignores EXECUTE). Three in-file proofs read back the ACLs, the authenticated doors and the four attached triggers. Linter 125 → 115; the remainder is ruled in admin-audit-security.md (five intentional anon catalog/gate reads, 108 gated admin doors, `pg_trgm` in `public` accepted, leaked-password protection = operator toggle). No src, no e2e.
- 2026-09-11 — DEC-022 HEALER: L2a MARK RAISED. Migration `20260911224612_17074bc4` (mark `20260912000000`, ledger only): the DEC-050 L2a file declared mark `20260911223000`, preceding its filename stamp `20260911223202` — the self-marking guard correctly failed CI. The file is append-only, so the ledger row is healed to `20260911223202` and the file is allowlisted citing this healer. `check-migrations.sh` green locally. No src, no e2e.
