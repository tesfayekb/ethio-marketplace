# C3 ERA GATE — FOUR-LENS REVIEW (G19) · 2026-09-09

Scope: the category + attribute era (C2–C5, C3a–C3g), the stabilisation arc (STAB-*, L4–L6, DEC-036…041), import/export (IE-1…IE-7, CAT-IE, IMPORT-GATE T1–T3), dependent options (DEC-045), inheritance (DEC-044/INH-1), the scanner findings (FIX-SCAN-1), and the Vehicles curation. Evidence: green push runs and nightly at 4–7 minutes per shard; judges 57014 = 0, auth errors = 0 across consecutive runs; 211-test closure run for the gate; Vehicles imported through preview → confirm with zero refusals after IE-6.

## Security
Proven: every import crosses the server boundary through one gate (permission, step-up, caps, encoding, headers, identity, cell hygiene, typed payload), the DB doors remain the sole authority (permission → step-up → capture → mutate), revisions tables are deny-all RLS, and the hostile-file spec runs per family (bidi/NUL/over-long/formula/unknown column/duplicate header/wrong identity/6k rows/>1 MB/invalid UTF-8/scope traversal/no permission/no step-up/digest mismatch/concurrent commit/foreign-actor undo). Deny cases proven for attributes (AT-14/23/24/34), categories (CT-22/23), translations (IG-4). Every re-declared definer carries in-file closers with ACL read-back (A8; three occurrences forged the guard). Session-per-test in node ended shared refresh-token exposure; token-derived step-up freshness ended the client hint. Residual: the inert E2E_UI_LOGIN knob in two workflows (DEC-048); INC-174 (local-only sign-out reconciliation) unresolved; the Supabase linter's standing "signed-in users can execute SECURITY DEFINER" class (by design — the gate is inside each function) is documented, not silenced. Recommendation: DEC-048 next; INC-174 before U6 (posting) because posting introduces the first unprivileged writers.

## Functionality
Proven: attributes normalised (definitions + links), library on the DataTable primitive with tiers, inherited rows visible with origin (primary lineage only), card-attribute law with the effective count, dependent options with cascading pickers, export/import for categories and attributes with column classes, guided refusals that name their values, undo, Amharic through the translation door as pending review; Vehicles curated end to end (17→18 categories, 43→56 definitions, root with zero direct links, nine of ten listing categories carded). Asserted, not yet proven in production use: the poster-facing cascade (U6), the feed filters (U7). Residual: Auto Services cards deferred to the Services pass; option-value translations (C3-UX-3) and other-text capture (C3-UX-4) open.

## Performance
Measured: shards 4–7 minutes (from 9–36); the i18n bundle is process-cached by publication version (18 ms → 0 within the TTL, a production win); DataTable pagination slices in the primitive; the roster RPC pre-aggregates (C3d). Cost added: the import gate parses on the server (bounded by the 1 MB / 5,000-row caps); effective-set walks are per request on admin pages only. Recommendation: none blocking; watch the nightly for the first regression of shard time above 8 minutes.

## Usability
Operator-verified by walk at 360 / 768 / 1024 / 1194 / 1280 / 1366: tiered columns with no clipping; ⋯ row menus; Used-by chips; the three-state import dialog; the Catalog group in the admin nav; refusals that say what to do. Amharic: labels for every surviving Vehicles definition pending review; the console's own keys EN+AM throughout. Residual: the cascade preview lives in the category editor, not a posting form (arrives with U6); the review workbook and the curation loop are operator-driven by design.

Verdict: C3 era CLOSED. Next: DEC-048 (knob deletion), the Real Estate curation loop, then U6 Posting per roadmap with DEC-043 (card attributes required at posting) as its first law.
