# Phase A (data foundations) — G19 close-out review · 2026-08-10

## Security

All new surfaces deny-by-default with admin paths via has_permission() from birth (DEC-013 §2). One deviation (INC-067: has_permission in an anon-reachable policy) caught in supervisor verification, class-ruled, fixed with an in-migration assertion pattern. Two new trigger laws (single-country, active-location), definer-guarded. Destructive restructure ran only under zero-reference fail-loudly assertions. Profile contact data owner+admin-only until B3's gated exposure function.

## Functionality

97 categories / 13 roots / 54 attributes; inheritance proven at every depth and semantically corrected (operator-caught INC-066): attributes at the narrowest node whose entire subtree they truthfully describe. Single-country law, exact-pin laws, alias/contact validation, and the column-grant boundary all behaviorally proven in-migration with restoration verified. Nothing asserted-only.

## Performance

Zero anon-policy cost (checked invariant post-INC-067, not convention). Plain doubles, no PostGIS (REQ-003). Capped recursive CTE per category view. No client code; bundle untouched.

## Usability

13 clean roots with icons throughout; sellers get alias, per-method contact visibility, prefill location — all data-driven for the C1 console (operator no-hard-coding directive). name_am = tracked launch-gate translation debt.

## Handed forward (named)

Owner-editable location states → D-spec confirmation. get_seller_card → B3. Account-type/business → E. Legacy profile seams (contact_prefs, viewing_location) → C1 ruling. Staging: consolidated five-migration sitting = the gate's final item.
